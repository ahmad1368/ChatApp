# Signs JWT access tokens issued by phone/OTP signup. If unset, the server
# generates an ephemeral secret at startup — fine for local dev, but every
# restart invalidates existing sessions. Set a real, stable value in any
# persistent deployment.
JWT_SECRET=
