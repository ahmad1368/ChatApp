# API origin the browser talks to (Socket.io + REST).
NEXT_PUBLIC_API_URL=http://localhost:4000

# App ID from a Meta for Developers app, used by the Facebook JS SDK to
# render a working Sign in with Facebook button. Leave unset to show a
# disabled "not configured" button instead of a broken one.
NEXT_PUBLIC_FACEBOOK_APP_ID=
