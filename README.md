# ChatApp

A dating app built around real-time chat, built incrementally from the project's 500-feature issue backlog, one issue at a time via the `/next-issue` workflow. See `CLAUDE.md` for the product-direction note.

## Stack

- **Monorepo:** npm workspaces (`apps/*`, `packages/*`)
- **Web:** Next.js (App Router) + TypeScript, PWA-first
- **API:** Node.js + Express + Socket.io (real-time messaging)
- **Shared:** `@chatapp/shared` — types and constants shared between web and API

See `CLAUDE.md` for scope decisions and the issue-implementation workflow.

## Getting started

```bash
npm install
npm run dev:api   # http://localhost:4000
npm run dev:web   # http://localhost:3000
```

## Implemented so far

- Baseline real-time chat (single room, Socket.io) — web UI + API scaffold
- #2 Real-time sync between mobile and web — reconnect/catch-up sync (`since` query param) so clients recover missed messages after a dropped connection, plus a live sync-status indicator
- #3 Responsive design for tablet and desktop — CSS custom properties + media-query breakpoints replace fixed inline layout
- #4 Local push-style notifications — Notification API alerts for new messages while the tab is backgrounded (foreground/open-tab equivalent of mobile push; full Web Push tracked separately in #5)
- #5 Web Push notifications — VAPID-backed subscribe/notify flow (service worker `push` handler, `/api/push/*` endpoints) so new messages can be delivered even when the tab is fully closed
- #6 Offline-first mode and data cache management — localStorage message cache for instant load, plus an outgoing-message queue that flushes automatically on reconnect
- #7 Battery and data usage optimization — WebSocket-only transport, tuned reconnection backoff, Data Saver opt-in, and auto-disconnect after the tab is hidden for 2 minutes
- #8 Dark / Light Mode support — CSS custom-property theme tokens, system-preference default, and a manual override toggle persisted in localStorage
- #9 Multilingual (i18n) and RTL/LTR support — English/Persian translations with a language switcher that flips `lang`/`dir` on `<html>` (RTL for Persian)
- #10 Deep linking — `/room/[roomId]` routing plus `?m=<messageId>` links that scroll to and highlight a specific message, with a per-message "copy link" action
- #11 Optimized REST API — backward-compatible cursor pagination (`limit`/`before`) on the messages endpoint, with a "Load older messages" client flow, instead of always shipping full room history
- #12 Image sharing with automatic compression — client-side canvas downscale/re-encode before upload, in-memory upload store, images rendered inline in the chat
- #13 CDN readiness for static assets — `NEXT_PUBLIC_CDN_URL`-driven `assetPrefix` plus immutable cache headers (build output and uploaded images), so a CDN can front the app without code changes
- #14 OS shortcuts integration — web app manifest `shortcuts` entry so installed PWAs get a jump-list/long-press shortcut into the app
- #15 Smooth 60fps swipe-to-reply — Pointer Events + transform/rAF-only dragging on message bubbles, swipe right to quote-reply
- #16 Keyboard shortcuts — Ctrl/Cmd+K to focus the composer, Shift+Enter for a new line, Esc to dismiss, and `?` for a shortcuts help overlay
- #17 Automatic crash/error reporting — global error/rejection handlers plus a React error boundary report to `/api/error-reports`, the web equivalent of Crashlytics
- #18 In-app update system — service worker update detection with an "Update now" prompt, the web equivalent of the Play Store in-app update flow
- #19 Data Saver Mode — a persistent, user-controlled toggle (defaulting to the OS Data Saver signal) that holds off loading messages/opening the live connection until the user asks for it
- #20 High-concurrency architecture readiness — optional Redis adapter for multi-instance Socket.io broadcasts, per-socket message rate limiting, and graceful shutdown
- #21 Phone number + OTP signup — `/signup` two-step flow, backend OTP request/verify with expiry/attempt limits/resend cooldown, JWT access + rotating refresh tokens
- #22 Google Sign-In — server-side ID token verification (`google-auth-library`), JWT session issuance, and a Google Identity Services button that gracefully degrades when unconfigured
- #23 Sign in with Apple — server-side ID token verification against Apple's JWKS, JWT session issuance, and a Sign in with Apple button that gracefully degrades when unconfigured
- #24 Sign in with Facebook — server-side access token verification via the Graph API's `debug_token`, JWT session issuance, gracefully-degrading button
- #25 Account recovery via email — `/recover` two-step flow reinterpreting "password recovery" for this passwordless app (SMS recovery is already covered by #21's phone OTP)
- #26 Two-factor authentication — TOTP (RFC 6238) setup with QR enrollment, confirm, verify, and disable, fully self-contained (no third-party 2FA provider needed); management endpoints gated behind a verified access token, demo UI at `/settings/security`
- #27 Biometric login (Face ID / fingerprint) via WebAuthn — real platform-authenticator registration and sign-in (no biometric data ever reaches the server, by design of the standard); registration gated behind a verified access token, login identifies the account via a device-remembered userId, demo UI at `/settings/security`
- #28 Step-by-step profile onboarding — server-persisted, resumable state machine (display name → avatar → bio) gated behind a verified access token, with a progress-bar UI at `/onboarding`
- #29 Dating goal selection — extends #28's onboarding state machine with a fourth step (marriage / friendship / casual chat), eHarmony-style choice cards; first feature of the app's dating-app pivot
- #30 Gender identity selection — extends the onboarding state machine with a fifth step offering diverse options (OkCupid-style) plus a custom free-text option and "prefer not to say"
- #31 Sexual orientation and match preferences — sixth onboarding step (diverse orientation options + who to be matched with), reusing the gender-options list for preferences
- #32 Preferred age range — seventh onboarding step, dual range sliders (18-99) validated against the legal minimum
- #33 Search radius — eighth onboarding step, distance slider plus opt-in browser geolocation with coordinates rounded server-side to ~1.1km precision before storage
- #34 Avatar upload and crop — replaces the onboarding avatar step's URL paste with a real upload + drag/zoom circular crop, backed by the existing #12 upload store
- #35 Automatic face detection in the main photo — on-device MediaPipe face detection during avatar crop, with a soft "use anyway" warning rather than a hard block
- #36 Live selfie verification — ninth onboarding step: live camera capture (never a picked file) + on-device face-presence check, stored privately with only a boolean "verified" flag ever exposed to clients, gated behind a verified access token
- #37 Verified badge — `GET /api/users/:userId/badge` (boolean-only, no image exposure, intentionally public/unauthenticated) plus a reusable blue-checkmark `VerifiedBadge` component shown after selfie verification
- #38 Guest mode with limited access — an entry choice screen (sign up vs. continue as guest, skipped entirely for an already-signed-in user); guests can read the chat but sending is disabled client-side and rejected server-side
- #39 Onboarding draft persistence across a sudden exit — `/onboarding` retries once with a refreshed access token (`fetchWithAuth`) instead of failing when a closed-tab-overnight return finds a stale 15-minute access token; the server already persists per-step progress against the real account (#28), so surviving a sudden exit only needed the session itself to survive one
- #40 Community Guidelines acceptance — new first onboarding step (ahead of display name), mandatory checkbox gate (no skip) recording an accepted-version number for future re-consent
- #41 User reporting system — categorized report reasons, a dependency-free `ReportStore` with its own rate limiting, a per-message "Report" action, and no read endpoint exposing stored reports
- #42 Block users to prevent re-encountering them — mutual, high-priority block/unblock API independent of any matching/discovery service, self-lookup only (no endpoint reveals who has blocked a given user); blocked pairs stop seeing each other's messages both in history (server-filtered) and live (client-filtered, since this app broadcasts to the whole room rather than per-socket to stay compatible with #20's Redis-backed multi-instance delivery)
- #43 Block phone contacts — users can self-declare a phone number (stored only as a SHA-256 hash, never in plaintext) and upload a contact list to auto-block any other author whose registered phone matches, reusing #42's mutual `BlockStore`; the web UI uses the Contact Picker API where supported (Chrome for Android) and falls back to manual number entry elsewhere (desktop/iOS have no bulk contact-export API)
- #44 Screenshot/DRM policy — browsers have no API to block or even detect an OS-level screenshot (no web equivalent of Android `FLAG_SECURE`/iOS screen-capture protection), so this implements the honest web mitigation: deterrence + traceability. `POST /api/watermark/session` issues a per-viewing-session trace code (never exposed via a read endpoint); the client renders it as a faint tiled watermark over the chat, and blurs chat content on tab-blur/visibility-change as defense-in-depth against shoulder-surfing/screen-share leaks
- #45 Custom photo watermarking to prevent photo theft — `PhotoStore` (5MB cap, jpeg/png/webp) + `POST /api/photos` upload; `GET /api/photos/:id` dynamically burns a viewer-labeled watermark into the image's actual pixel data on every serve (via `jimp`, no native image-library dependency), so a leaked copy of the raw file itself carries the watermark rather than a strippable DOM overlay
- #46 Safety Center and guide for safe meetups — `/safety` page with static safe-meetup tips; its "Share your date" section now links to #47's `/share-my-date` flow (reconciled per CLAUDE.md's known-overlap note — #46's original single-link `SafetyPlanStore` is retired in favor of #47's richer `SharedDateStore`)
- #47 Share My Date — canonical "share your date" implementation: `SharedDateStore` issues a distinct share code per named trusted contact (`POST /api/shared-dates`), lets the sharer push live status updates (`PATCH /api/shared-dates/:id/status`: planned/on the way/arrived/safe/need help) that every contact sees on refresh, and supports revoking all access at once (`POST /api/shared-dates/:id/revoke`); `/share-my-date` is the sharer's UI, `/share-my-date/shared/[code]` is the trusted contact's read-only view
- #48 Emergency SOS — a prominent SOS button in the chat UI uses the browser's Geolocation API to capture the sender's current position and trigger an alert (`SOSStore`) shared with every registered emergency contact via a distinct, live-updating share code each (stubbed notification delivery, same pattern as other stubbed third-party sends); the sender can push location updates while the alert is active and resolve it once safe, and contacts view a live, auto-refreshing `/sos/shared/[code]` page with no login required
- #49 Biometric re-authentication when entering the app — WebAuthn platform authenticators (Touch ID/Windows Hello/Android fingerprint via Chrome) are the real, working web equivalent of native biometric re-auth; `WebAuthnStore` (`apps/api/src/webauthn.ts`, `@simplewebauthn/server`) enrolls a `platform`-attachment, `userVerification: "required"` credential per guest identity and re-verifies it whenever the tab becomes visible again. A persistent guest identity (`localStorage`, `apps/web/src/app/guestIdentity.ts`) replaces the previous per-reload random author, since re-authentication needs something to re-authenticate *against*. `BiometricLock.tsx` gates the chat UI: first visit offers enrollment (skippable), later visits/returns require an `@simplewebauthn/browser` re-auth before revealing content
- Complete account and data deletion (GDPR erasure) — `DELETE /api/account/:author`, web page at `/privacy`
- Download a backup of personal data (GDPR export) — `GET /api/account/:author/export`, web download page at `/privacy/export`
- Hide exact location and show only an approximation (~5km) — `PUT`/`GET /api/users/:author/location`, web page at `/privacy/location`
- Full PWA support for the browser — web app manifest, installable icons, and an offline-capable service worker with an offline fallback page
- #53 Detect duplicate/intrusive accounts based on IP and device fingerprint — `DuplicateAccountStore` links accounts that share a hashed IP address or a client-declared device fingerprint (never the raw values), flagging both sides for review rather than blocking sign-in outright (too many false positives from shared wifi/VPNs to act on automatically); wired into every sign-in path (phone OTP, Google, Apple, Facebook) via `GET /api/auth/duplicate-status`, surfaced to the affected user themselves as a heads-up on `/settings/security`
- #54 Don't show profile to people from the same city/workplace (if configured) — Bumble's real "hide my profile from members of my workplace/school" preference, generalized to a self-reported city too; `DiscoveryVisibilityStore` (`PUT`/`GET /api/discovery-visibility`, gated behind a verified access token) stores the opt-in toggle and the pure visibility check a future discovery/matching feature will call — this app has no discovery feed yet, so this stores the preference ahead of that, the same scoping already used for #33's search radius; settings UI at `/settings/discovery-visibility`
- #55 Automatically scan profile text to prevent sharing a phone number or address — `scanForContactInfo()` (`apps/api/src/contactInfoDetector.ts`) heuristically detects phone numbers (digit runs of 7+, with or without separators) and street addresses (a house number plus a street-type word) in free text; wired into onboarding's `displayName` and `bio` steps, which now reject saving with an editable error rather than silently stripping anything
- #56 Strict policy against financial and crypto scams — `scanForScamContent()` (`apps/api/src/scamDetector.ts`) detects crypto wallet addresses (Bitcoin legacy/bech32, Ethereum) and common investment-scam phrasing in chat messages; the `message:send` socket handler now rejects a flagged message (`message:rejected` with reason `scam_content`) the same way it already rejects a rate-limited or guest-mode send, and the client surfaces a plain-language explanation instead of the message silently vanishing
- #57 Automatic bot detection and blocking (reCAPTCHA) — `RecaptchaService` (`apps/api/src/recaptcha.ts`) verifies a reCAPTCHA v3 token against Google's siteverify endpoint (score-threshold check, same injectable-verifier pattern as #22-#24's OAuth providers) and gates `POST /api/auth/signup/request-otp`, the one signup step that costs real money to abuse; gracefully degrades to a no-op when `RECAPTCHA_SECRET_KEY`/`NEXT_PUBLIC_RECAPTCHA_SITE_KEY` aren't configured, so phone-OTP signup — this app's only always-available sign-in method — never breaks because of it
- #58 Report spam or promotional messages to the monitoring system — `scanForSpamContent()` (`apps/api/src/spamDetector.ts`) detects links and common promotional phrasing in chat messages; unlike #56's scam check, a match doesn't block the send (Tinder's real behavior is to route it to moderation, not break the conversation over a promotional link) — instead it auto-files a `spam`-reason report into #41's existing `ReportStore`, the "monitoring system" already in this codebase
- #59 Set photo album access level (public, private, request access) — Bumble's real "Private Album" control, generalized to three tiers; `PhotoAlbumStore` (`PUT`/`GET /api/photo-albums/:owner/access-level`) gates #45's photo serve endpoint, and a request/approve/deny flow (`POST /api/photo-albums/:owner/access-requests`, `.../access-requests/:requester/respond`) covers the request-access tier; UI lives alongside the existing photo-upload section in the chat room
- #60 Log active sessions with the ability to log out of other devices — `TokenService` (`apps/api/src/auth.ts`) now tracks one session per sign-in, surviving every access-token refresh instead of treating each renewal as a new login; `GET /api/auth/sessions` lists them with a friendly device label (`describeUserAgent()`, parsed from the `User-Agent` header) and marks the caller's own, `DELETE /api/auth/sessions/:sessionId` logs out one device, `DELETE /api/auth/sessions/others` logs out every device but the current one; UI at `/settings/sessions`
- #61 Photo album with the ability to upload up to 9 photos (Hinge) — `PhotoAlbumStore` now also tracks an ordered, capped list of album photo ids per owner (`POST`/`GET /api/photo-albums/:owner/photos`, `DELETE .../photos/:photoId`), reusing #45/#59's existing upload and per-photo access-level gating rather than duplicating them; the chat room's photo section is now a 3x3 grid with per-photo removal instead of a single slot
- #62 Reorder photos via drag & drop — `PUT /api/photo-albums/:owner/photos/order` (validated as a rearrangement of the exact same photo set, never a way to add/drop one) backs native HTML5 drag-and-drop on the #61 photo grid, reordering optimistically and re-syncing from the server if the request fails
- #63 Ability to upload a short intro video (looping video/reels) — `IntroVideoStore` stores one short clip per profile (`POST`/`GET`/`DELETE /api/intro-video`(`/:author`), mp4/webm, 20MB cap), deliberately simpler than the photo album (no per-frame watermarking, no multi-clip/access-level model — a video-processing pipeline for that is out of scope here); the client checks duration (≤30s) before upload since the server can't decode video without a heavy dependency, then plays it back looping/muted/autoplaying like a real profile intro clip
- #64 Record and upload a 30-second voice intro (Hinge) — `VoiceIntroStore` is the same one-clip-per-profile shape as #63's intro video, scoped to audio mime types and a 5MB cap; the client records directly in-browser via `MediaRecorder` (`getUserMedia({ audio: true })`), auto-stopping at 30 seconds, then uploads and plays the clip back through `POST`/`GET`/`DELETE /api/voice-intro`(`/:author`)
- #65 Write a text bio with a character limit (Hinge) — `BioStore` (`PUT`/`GET /api/bio/:author`) is an editable-anytime 280-character bio, independent of the one-time bio collected during onboarding, reusing the same contact-info-detection rule (no phone numbers/addresses); the web profile section gets a `BioEditor` textarea with a live remaining-character count
- #66 Answer ready-made profile prompts to start a conversation (Hinge) — `ProfilePromptsStore` lets a profile pick up to 3 prompts from a fixed catalog (`GET /api/profile-prompts/catalog`) and answer each (150 chars, reusing the same contact-info-detection rule as other free-text profile fields), stored/replaced as one set per author (`PUT`/`GET /api/profile-prompts/:author`); starting a conversation from an answer reuses the existing chat surface rather than a separate "reply to this prompt" message type
- #67 Show job and workplace (Hinge) — `JobInfoStore` (`PUT`/`GET /api/job-info/:author`) is an editable-anytime, optional job title + company pair, reusing the same contact-info-detection rule as other free-text profile fields; includes a `hideCompany` flag mirroring Hinge's real privacy option to show your title while hiding the specific employer
- #68 Show education and university (Hinge) — `EducationInfoStore` (`PUT`/`GET /api/education-info/:author`) is an editable-anytime, optional school field with the same `hideSchool` privacy flag pattern as #67's job info
- #69 Show height and physical characteristics (Hinge) — `HeightInfoStore` (`PUT`/`GET /api/height-info/:author`) is an editable-anytime, optional height field (stored in cm, 100-250 range) with the same `hideHeight` privacy flag pattern as #67/#68
- #70 Show smoking and drinking status (Hinge) — `LifestyleInfoStore` (`PUT`/`GET /api/lifestyle-info/:author`) stores two independent fixed-choice enums (smoking: no/sometimes/yes; drinking: no/sometimes/yes/onSpecialOccasions), each with its own `hideSmoking`/`hideDrinking` privacy flag
- #71 Show plans for having children (Hinge) — `FamilyPlansInfoStore` (`PUT`/`GET /api/family-plans-info/:author`) stores a fixed-choice "family plans" enum (dontWantChildren/wantChildren/openToChildren/notSureYet) with the same `hideFamilyPlans` privacy flag pattern as #67-#70
- #72 Show zodiac sign and birth month (Hinge) — `ZodiacInfoStore` (`PUT`/`GET /api/zodiac-info/:author`) stores an optional birth month/day pair and derives the zodiac sign server-side (no year needed/stored), with the same `hideZodiac` privacy flag pattern as #67-#71
- #73 Show languages the user is fluent in (Hinge) — `LanguagesInfoStore` (`GET /api/languages-info/catalog`, `PUT`/`GET /api/languages-info/:author`) lets a profile pick up to 5 languages from a fixed catalog, same fixed-catalog pattern as #66's profile prompts, with a `hideLanguages` privacy flag
- #74 Show religion, beliefs and political views (Hinge) — `BeliefsInfoStore` (`PUT`/`GET /api/beliefs-info/:author`) stores two independent fixed-choice enums (religion, political view), each with its own hide flag since these are especially sensitive protected-category fields
- #75 Show pet status (Hinge) — `PetsInfoStore` (`GET /api/pets-info/catalog`, `PUT`/`GET /api/pets-info/:author`) lets a profile pick up to 3 pet-status options from a fixed catalog (dog/cat/bird/.../no pets/pet-free/want a pet/allergic), same fixed-catalog multi-select pattern as #73's languages, with a `hidePets` privacy flag
- #76 Show personality type (MBTI/Enneagram) (Hinge) — `PersonalityInfoStore` (`PUT`/`GET /api/personality-info/:author`) stores an optional MBTI type (16-value enum) and an optional Enneagram type (1-9), each independently hideable
- #77 Connect Spotify account to show favorite music (Hinge) — `SpotifyService` (`spotifyAuth.ts`) exchanges a client-obtained OAuth authorization code for an access token and fetches the user's top 5 tracks from Spotify's Web API, same injectable-verifier/`isConfigured()` shape as #23-#25's Google/Apple/Facebook sign-in; `SpotifyInfoStore` persists the connected state (`POST /api/spotify/connect`, `DELETE /api/spotify/:author`, `PUT`/`GET /api/spotify-info/:author`)
- #78 Connect Instagram to show latest posts (Hinge) — `InstagramService` (`instagramAuth.ts`) exchanges a client-obtained OAuth authorization code for an access token and fetches the user's 5 latest posts from the Instagram Basic Display API, same shape as #77's Spotify connect; `InstagramInfoStore` persists the connected state (`POST /api/instagram/connect`, `DELETE /api/instagram/:author`, `PUT`/`GET /api/instagram-info/:author`)
- #79 Choose interests and lifestyle from ready-made tags (Hinge) — `InterestsInfoStore` (`GET /api/interests-info/catalog`, `PUT`/`GET /api/interests-info/:author`) lets a profile pick up to 10 interest tags from a fixed catalog, same fixed-catalog multi-select pattern as #73's languages and #75's pets, with a `hideInterests` privacy flag
- #80 Hide specific profile sections (such as age or distance) (Feeld) — `ProfileVisibilityStore` (`PUT`/`GET /api/profile-visibility/:author`) adds `hideAge`/`hideDistance` toggles; unlike #67-#79's per-optional-detail hide flags, age and distance are core fields shown by default and had no existing hide control
- #81 Preview profile as seen by other users (Hinge) — `buildProfilePreview` (`profilePreview.ts`, `GET /api/profile-preview/:author`) is a pure function that composes every standalone profile field from #61-#79 (bio, job, education, height, lifestyle, family plans, zodiac, languages, beliefs, pets, personality, Spotify, Instagram, interests) into the single view another user would actually see, respecting each field's own hide flag and omitting hidden/empty fields entirely rather than sending null placeholders
- #82 Smart photo optimization (Hinge) — `optimizePhoto` (`photoOptimization.ts`) runs on every `POST /api/photos` upload: downscales any photo over 1600px on its longest edge and always re-encodes as JPEG, which as a side effect strips EXIF metadata (including GPS location tags a phone photo can carry) — a real privacy backstop for the client-side compression in `imageCompression.ts`, not just smaller files
- #83 Add personal social media links to profile (Tinder) — `SocialLinksInfoStore` (`GET /api/social-links-info/platforms`, `PUT`/`GET /api/social-links-info/:author`) stores optional https:// URLs for a fixed set of platforms (X/Twitter, TikTok, YouTube, LinkedIn, personal website), unlike #77/#78's OAuth-based Spotify/Instagram connects these are just self-typed links validated as https:// URLs, with a `hideSocialLinks` privacy flag
- #84 Show work/travel mode (Hinge) — `TravelModeInfoStore` (`PUT`/`GET /api/travel-mode-info/:author`) is Tinder Passport/Bumble Travel Mode's real "I'm temporarily somewhere else" toggle plus an optional destination string, reusing the same contact-info-detection rule as other free-text fields; no separate hide flag since the active/inactive toggle already is the show/hide control
- #85 Choose a color theme for the profile (Tinder) — `ProfileColorThemeStore` (`GET /api/profile-color-theme/themes`, `PUT`/`GET /api/profile-color-theme/:author`) is a fixed 6-option accent-color enum for the profile card, distinct from `ThemeToggle.tsx`'s app-wide light/dark UI theme
- #86 Measure profile completion percentage (Hinge) — `computeProfileCompletion` (`profileCompletion.ts`, `GET /api/profile-completion/:author`) is Hinge/LinkedIn's real "profile strength" nudge: a pure function checking how many of 14 profile sections (photo, bio, job, education, ...) are filled in, reusing the same per-field getters as #81's preview but ignoring hide flags entirely — a hidden-but-filled field still counts toward completion, since this measures what's filled in, not what's currently visible
- #87 Add resume documents or official achievements (optional) (Match.com) — `AchievementsInfoStore` (`PUT`/`GET /api/achievements-info/:author`) stores up to 5 short achievement entries (title/issuer/year), reusing the same contact-info-detection rule as other free-text fields; raw resume/PDF document upload is deliberately out of scope (documented in achievementsInfo.ts) since this codebase has no malware/content scanning for arbitrary document uploads and a resume PDF is far more PII-dense than any other upload this app accepts
- #88 Set how the name is displayed (first name, initials, nickname) (Hinge) — `DisplayNameModeStore` (`GET /api/display-name-mode/modes`, `PUT`/`GET /api/display-name-mode/:author`) and a pure `resolveDisplayName` helper let a profile choose how #21's onboarding name is rendered to others (full name/first name only/initials/nickname), independent of onboarding.ts the same way #65's bio is
- #89 Support for 3D and cartoon avatars (Hinge) — `StylizedAvatarStore` (`GET /api/stylized-avatar/styles`, `PUT`/`GET /api/stylized-avatar/:author`) lets a profile pick a preset stylized-avatar id and toggle showing it instead of their photo; this app has no 3D rendering/asset-generation pipeline, so "support" here means a fixed preset catalog (same scoping call as #85's color themes) rather than real 3D avatar creation
- #90 Quickly edit and update details from settings (Hinge) — new `/settings/profile` page consolidates all 26 standalone profile-field editors (#61-#89: bio, intro video/voice, prompts, job/education/height/lifestyle/family-plans/zodiac/languages/beliefs/pets/personality, Spotify/Instagram connect, interests, visibility, social links, travel mode, color theme, achievements, display-name mode, stylized avatar, completion meter, preview) that previously lived permanently inline at the bottom of the chat screen; ChatRoom now links out to it instead. The photo album section stayed in ChatRoom — it's built directly into that component's own state/handlers rather than a standalone `author`-prop component, so moving it is a separate, riskier refactor
- #91 Swipe card interface (Swipe Left / Swipe Right) (Tinder) — first "Matching Algorithm & Discovery" feature. `SwipeStore` (`swipes.ts`) is the actual decision/match engine: an opt-in candidate pool (`POST /api/discovery/join`), a like/pass decision per candidate (`POST /api/swipes`) with mutual-like match detection (`GET /api/matches/:author`), and candidate filtering against #16's `BlockStore` so blocked authors never appear in each other's queues; a new `/discover` page renders it as Like/Pass buttons (plus arrow-key support) rather than a draggable gesture card, since this repo has no gesture-physics dependency — the implementation guide's Kafka/Redis-backed recommendation service and native 60fps gesture layer are documented as out of scope in swipes.ts
- #92 Undo the last like or dislike (Rewind feature) (Tinder) — `SwipeStore.undoLastSwipe` (`POST /api/swipes/undo`) undoes only the single most recent swipe (matching the issue's literal "the LAST" wording, not a full history), re-opens that candidate, and revokes the match too if that swipe had just created one; free here since this app has no premium/paywall tier gating it like real Tinder Gold does. `/discover` gets a Rewind button (also bound to the Up arrow) that only appears after a swipe
- #93 Send a Super Like for special attention (Tinder) — `SwipeStore` gains a `superlike` swipe direction that counts toward a match same as an ordinary like, is rate-limited to 1/day per author (refunded if rewound via #92), and surfaces authors who've superliked you first in `getCandidates` — the actual "special attention" the feature promises, via `GET /api/super-likes-remaining/:author`; `/discover` gets a ⭐ button (also bound to the Down arrow) showing the remaining daily count
- #94 Matching algorithm based on interest vector average (Vector Matching) (OkCupid) — `computeInterestCompatibility` (`interestCompatibility.ts`) is OkCupid's real percentage-match score, computed literally per the issue's own title: shared #79 interest tags divided by the average of the two profiles' interest-vector sizes; `SwipeStore.getCandidates` gains an injected compatibility scorer used as the secondary sort key (after #93's superliker priority), and `GET /api/swipe-candidates/:author` now returns `{author, compatibility}` pairs instead of bare names — `/discover` shows the resulting "X% match" badge on each card
- #95 Attractiveness and activity scoring algorithm (Elo / Smart Score) (Tinder) — `SmartScoreStore` (`smartScore.ts`) implements Tinder's real (in)famous Elo Score/Smart Score: every swipe applies a standard chess-Elo update to the swiped author's desirability rating (a like from a higher-rated swiper raises it more than one from a lower-rated swiper) plus a separate per-author activity count, exposed via `GET /api/smart-score/:author`; shown only to the profile owner on `/settings/profile`, never on `/discover` alongside candidates, since surfacing another profile's score would let people rank/filter each other by it — the exact outcome real Tinder kept this hidden to avoid
- #96 Advanced filter by height, education and language (OkCupid) — `DiscoveryFiltersStore` (`discoveryFilters.ts`) lets each author set a height range, an "only show people with education listed" toggle, and a required-languages list; `candidateMatchesFilters()` excludes any `/api/swipe-candidates/:author` result that fails them, reading the candidate's raw height/education/language data regardless of that candidate's own hide-on-my-profile flags (same precedent as #94's interest compatibility), with missing data failing an active filter; edited via `DiscoveryFiltersEditor` on `/settings/profile`
- #97 Filter by non-smoking and lifestyle (OkCupid) — extends #96's `DiscoveryFiltersStore`/`candidateMatchesFilters()` with a "only show non-smokers" toggle and an allowed-drinking-habits list, reading #70's `LifestyleInfoStore` smoking/drinking data the same way #96 reads height/education/language (raw data regardless of hide flags, missing data fails an active filter); same `PUT`/`GET /api/discovery-filters/:author` routes and `DiscoveryFiltersEditor` UI gain the new controls
- #98 Filter users with a verified identity badge (Verified Only) (OkCupid) — extends #96/#97's discovery filters with `requireVerifiedOnly`, checked against #35's real `VerificationStore.isVerified()` selfie-verification signal; documented gap (see `discoveryFilters.ts`): this app's guest "author" identity (used by the whole discovery/swipe subsystem) and its real authenticated `userId` (used by accounts/verification) aren't unified yet, so the filter is wired correctly today but only reflects real verifications once a guest author is linked to its account, same disclosure style as `verification.ts`'s own liveness-detection gap
- #99 Thematic discovery mode (Explore Mode: cafes, sports, travel) (Tinder) — `ExploreModeStore` (`exploreMode.ts`) lets each author pick one active theme (or none); `candidateMatchesExploreMode()` narrows `/api/swipe-candidates/:author` to candidates sharing at least one of that theme's curated #79 interest tags (cafes → coffee/foodie/baking/wine/reading, sports → gym/running/cycling/hiking/yoga, travel → travel/camping/photography/hiking); `PUT`/`GET /api/explore-mode/:author` plus `GET /api/explore-mode/catalog`, picked via `ExploreModeSelector` on `/discover`
- #100 Show mutual compatibility percentage (Compatibility Score) (OkCupid) — new `/matches` page lists each of an author's matches with their #94 `computeInterestCompatibility` score shown persistently ("X% compatible"), distinct from #94's transient swipe-card badge that disappears once you swipe past a candidate; `GET /api/matches/:author` now returns `{author, compatibility}` pairs instead of bare names, computed at the route level the same way #94's swipe-candidates route does it; links into the app's single shared chat room since per-match DM rooms don't exist yet in this codebase (noted as separate, larger scope)
- #101 Daily suggestion of top users (Top Picks) (Tinder) — `TopPicksStore` (`topPicks.ts`) ranks the same eligible candidate pool `/api/swipe-candidates` draws from (blocks + #96-#99's discovery filters already applied) by #95's real Elo/Smart Score desirability rating — real Tinder documents Top Picks as powered by that same signal — and caches the top 5 per author until the UTC date changes, so repeat requests the same day return identical picks; `GET /api/top-picks/:author`, shown via a new `TopPicks` component on `/discover`
- #102 Passport mode to change location to other cities (Tinder) — extends `LocationStore` (`locationPrivacy.ts`) with a real, functional location override (`setPassportLocation`/`clearPassportLocation`/`getEffectiveLocation`), distinct from #84's `TravelModeInfo` which is only a cosmetic "I'm elsewhere" profile badge with no effect on actual discovery location; `GET /api/users/:author/location` now returns the active Passport override when set, falling back to the real GPS location otherwise; new `PUT`/`DELETE`/`GET /api/users/:author/passport-location` routes, edited via `PassportModeEditor` on `/settings/profile` (city coordinates entered directly — no geocoding service exists in this app)
- #103 View the list of people who liked you (Liked You) (Tinder) — `SwipeStore.getLikedBy()` returns everyone who liked/superliked this author but hasn't been swiped back on yet, ranked the same way as `getCandidates` (superlikers first, then by #94 compatibility); free here since this app has no premium tier to gate it behind (same call as #92's free Rewind), and deliberately skips the swiper's own discovery filters/explore mode (those narrow who *you* go looking for, not who already expressed interest in *you*) while still respecting blocks; `GET /api/liked-you/:author`, shown on a new `/liked-you` page with inline like/pass buttons that immediately create a match on mutual like
- #104 View profile visitors in the last 24 hours (Tinder) — new `ProfileVisitsStore` (`profileVisits.ts`) tracks visits via an optional `?viewer=` query on the existing `GET /api/profile-preview/:author` (#81), keeping only the most recent visit per visitor and pruning anything older than 24 hours on read; self-visits are never recorded; new `GET /api/profile-visitors/:author` route, a new `/profile/[author]` page for viewing someone else's profile (distinct from `/settings/profile`'s self-preview), and a `/visitors` page listing who viewed you, linked from candidate/match/liked-you names across `/discover`, `/matches`, and `/liked-you`; free here since this app has no premium tier to gate it behind, same call as #103
- #105 Profile Boost to increase visibility (Tinder) — new `ProfileBoostStore` (`profileBoost.ts`) activates a 30-minute boost per author (re-activating resets rather than stacks the window); `SwipeStore.getCandidates()` gains an `isBoosted` predicate ranking a boosted candidate ahead of everyone except someone who's already superliked the viewer (a personal signal outranks a general visibility one); new `POST`/`GET /api/profile-boost/:author` routes, a live countdown `ProfileBoost` button on `/discover`; free here since this app has no premium tier to gate it behind, same call as #92's free Rewind
- #106 Smart boost during peak network hours (Super Boost) (Tinder) — extends #105's `ProfileBoostStore` with a `superboost` tier that outranks a plain `boost` in `SwipeStore.getCandidates()`'s priority ordering (`getBoostLevel()`, 0/1/2); the "smart" half is a new `PeakHoursStore` (`peakHours.ts`) that builds an honest UTC-hour-of-day histogram from real recorded swipe activity (not a fabricated ML model) and exposes `GET /api/peak-hours` with the busiest hours and whether right now is one of them; `ProfileBoost` on `/discover` gains a tier picker and a "🔥 peak hour" hint when it's a good time to boost
- #107 Automatic algorithm to remove spam and fake profiles from the like queue (Bumble) — new `fakeProfileDetector.ts` combines two signals this app already collects rather than inventing an ML model: a candidate reported `REPORT_THRESHOLD` (3) or more times (reusing `ReportStore.countFor()`, previously unwired and explicitly earmarked for this) or whose bio matches the existing chat-message spam heuristic (`spamDetector.ts`'s promo-phrase/URL scan, now applied to bios too) is excluded from the like queue via `isExcludedCandidate`, shared by `/api/swipe-candidates` and `/api/top-picks`; silent and automatic like real apps' moderation — no UI, since there's nothing for an excluded profile's owner or a viewer to see
- #108 Matching based on live location and route intersection (Crossed Paths) (Tinder) — new `CrossedPathsStore` (`crossedPaths.ts`) records a location "ping" each time `PUT /api/users/:author/location` (#33/#102) is called, then flags two authors as having "crossed paths" when any of their pings within the last 24 hours came within 500m of each other (`haversineDistanceKm`, reused from `locationPrivacy.ts`); an honest limitation given this app has no continuous background GPS tracking, disclosed rather than faked; `GET /api/crossed-paths/:author` draws from the same eligible pool as `/api/swipe-candidates`/`/api/top-picks`, shown via a new `CrossedPaths` section on `/discover`
- #109 Group matching for multi-person dates (Double Dating) (Match.com) — new `SquadStore` (`squads.ts`) lets 2-4 friends team up as one squad and swipe on other squads together (`POST /api/squads`, `POST /api/squad-swipes`); a mutual like opens one shared group chat room reusing this app's existing multi-room chat rather than a separate group-chat system; scoped to the actual group-matching mechanic (create/discover/swipe/match/shared room) with no separate group-compatibility scoring invented; new `/double-date` page to create a squad, swipe on other squads, and jump into the resulting group chat
- #110 Show whether a user is online or their last active time (Badoo) — new `PresenceStore` (`presence.ts`) tracks a per-author live connection *count* (not a boolean, so a second open tab doesn't flicker the first tab's close to "offline") plus the most recent moment they were connected or sent a message; `server.ts`'s socket layer calls it from a new `presence:online` event (client announces identity once per connection), the existing `disconnect` handler, and `message:send`; exposed via `GET /api/presence/:author` and shown on `/profile/[author]` as "🟢 Online now" or "Active Xm/h/d ago", polled every 30s since that page doesn't already hold a socket connection
- #111 Vanish/incognito mode (Bumble) — new `VanishModeStore` (`vanishMode.ts`) is a per-author on/off toggle hiding that author from everyone's discovery/top-picks queue *except* someone they've already liked or superliked themselves (new `SwipeStore.hasLiked()`), matching Bumble's real "stay dark to strangers, still reachable by people you deliberately swiped on" behavior rather than a blanket invisible toggle that would make matching impossible; composed into `isExcludedCandidate` alongside #96's filters and #107's fake-profile scan; `PUT`/`GET /api/vanish-mode/:author`, toggled via a new `VanishModeEditor` on `/settings/profile`
- #112 Ability to send a note or like on a specific profile photo (Hinge-style, listed as Tinder) — new `PhotoInteractionStore` (`photoInteractions.ts`) lets a viewer like (toggle) or leave a short text note on one specific photo in someone's album (#59-#62), distinct from swipes.ts's whole-profile like/pass; gated by the same block check and #59 album-access-level check as #45's photo serve, plus confirming the photoId is actually in that owner's album; `POST`/`GET /api/photo-likes/:owner/:photoId` and `/api/photo-notes/:owner/:photoId`; shown via a new `ProfilePhotoGallery` on `/profile/[author]` rendering each album photo with a heart toggle and note thread
- #113 Search based on keywords found in bio (Tinder) — new `bioMatchesKeyword()` (`bioSearch.ts`) is a plain case-insensitive substring match against #65's bio field, no search index/tokenizer built for it; wired as an optional `?bioKeyword=` query on `GET /api/swipe-candidates/:author` composed into the existing candidate-exclusion pipeline (so it narrows the same ranked pool rather than filtering after the fact) — a one-off query rather than a persisted preference like #96's discovery filters; new search box on `/discover`, with the existing arrow-key swipe shortcuts now ignoring keydowns while that input is focused
- #114 Matching based on shared contacts or friends list (Tinder) — new `ContactsGraphStore` (`contactsGraph.ts`) rebuilds Tinder's real Facebook-friends "X mutual friends" signal on #17's phone-contact hashing (`hashPhone`, now exported from `contactBlocks.ts`) since this app has no Facebook Graph API integration: an author uploads their phone contacts, and any other candidate sharing at least one hashed contact is surfaced; `POST /api/contact-graph/:author` (upload, replaces rather than merges) and `GET /api/shared-contacts/:author` (drawn from the same eligible pool as `/api/swipe-candidates`/#108's Crossed Paths); new `SharedContacts` "People you may know" section on `/discover` with a paste-your-contacts box
- #115 Show profiles as a grid or list on web (Tinder) — new `ViewModeStore` (`viewMode.ts`) is a persisted per-author display preference (card/grid/list), same shape as #99's `ExploreModeStore`, so the choice follows the author across sessions rather than resetting on every visit; `PUT`/`GET /api/view-mode/:author`; `/discover` gained a view toggle — grid/list modes render every fetched candidate at once (OkCupid's real DoubleTake-style browsing) with inline Like/Pass buttons per tile instead of one swipe card at a time, reusing the same `swipe()` decision path with a per-candidate target instead of always acting on `candidates[0]`
- #116 Daily like limit for free users (Coffee Meets Bagel) — `swipes.ts` gains `DAILY_LIKE_LIMIT` (100, Tinder's real free-tier cap), tracked with the same per-direction date+count shape #93's Super Like limit already used (now shared via small `usedToday`/`incrementUsage`/`refundUsage` helpers rather than duplicating that logic a second time); a Super Like never counts against it and `pass` is never limited, matching real Tinder; `undoLastSwipe` refunds a like the same way it already refunded a Super Like; new `GET /api/likes-remaining/:author`; `/discover`'s Like button now disables at zero remaining, same treatment the Super Like button already got
- #117 Reset profile queue after finishing the local list (Coffee Meets Bagel) — real Tinder's own queue-refill behavior: `SwipeStore.getCandidates()` now recycles previously-**passed** profiles back into the pool once the never-swiped pool is empty (a like/superlike/match is a decision already made and is never recycled); `recordSwipe()` correspondingly allows a fresh swipe to overwrite an existing "pass" (any other prior direction still can't be re-swiped), so a recycled profile can be liked into a match if the other side already liked back; entirely transparent server-side behavior — the existing `/discover` swipe UI needed no changes, matching how real apps refill the queue silently rather than showing a "restart" prompt
- #118 Suggestions based on shared favorite music (Hinge) — new `computeMusicMatch()` (`musicMatch.ts`) applies Hinge's real "you both like X" shared-interest framing to #77's Spotify top tracks, reusing #94's generic percentage-overlap formula (`computeInterestCompatibility`) rather than a second scoring algorithm and additionally surfacing *which* tracks are shared; `GET /api/music-matches/:author` (same drawn-from-the-same-pool shape as #108/#114) requires both sides connected and respects #77's own `hideSpotify` flag; new `MusicMatches` "🎵 Shared music taste" section on `/discover`
- #119 Suggestions based on weekend date plans (Tinder) — new `WeekendPlansStore` (`weekendPlans.ts`) is a "what are you up to this weekend" prompt, same fixed-catalog multi-select/hide-flag shape as #79's interests; new `computeWeekendPlanMatch()` (`weekendPlanMatch.ts`) reuses the same #94 overlap formula as #118's music matches to surface shared plans; `PUT`/`GET /api/weekend-plans/:author` (+ `/catalog`) to set them and `GET /api/weekend-plan-matches/:author` to discover candidates sharing at least one, drawn from the same eligible pool as #108/#114/#118; new `WeekendPlansEditor` on `/settings/profile` and `WeekendPlanMatches` "📅 Same weekend plans" section on `/discover`
- #120 Use AI to analyze bio text and improve matching (Hinge) — implemented as an honest keyword-extraction heuristic (`extractKeywords()`/`computeBioMatch()` in `bioAnalysis.ts`: lowercase, strip punctuation, drop stopwords/short words, dedupe) rather than an invented LLM/NLP integration this app has no model or inference infrastructure for, same scoping call as #95's Elo-style Smart Score and #107's fake-profile heuristic; reuses #94's overlap formula over each bio's extracted keyword set to surface which words two bios share; `GET /api/bio-matches/:author` (same drawn-from-the-same-pool shape as #118/#119); new `BioMatches` "✨ Similar bios" section on `/discover`
- #121 Instant text chat via WebSocket / Socket.io (Bumble) — this is this app's original chat core (predates the dating-app backlog entirely: `message:send`/`message:new`/`join` in `server.ts`, `ChatRoom.tsx` on the client), not something to build from scratch; the one real gap against this issue's own acceptance criteria was that it had only ever been tested through REST message-history endpoints, never through an actual socket.io connection — new `chatSocket.test.ts` drives `createChatServer()` with a real `socket.io-client` (added as a devDependency) covering room-scoped broadcast, cross-room isolation, and the guest-mode/scam-content/rate-limit rejection paths end-to-end
- #122 Send voice notes with a waveform (Badoo) — new `VoiceNoteStore` (`voiceNotes.ts`) is a many-per-room upload store (unlike #64's one-per-profile `VoiceIntroStore`), keyed by generated id same as `uploads.ts`'s chat images; `ChatMessage`/`SendMessagePayload` gain `audioUrl`/`waveform` fields carried straight through `buildChatMessage`; the waveform (an array of 0-1 amplitude samples) is computed client-side via the Web Audio API (new `voiceNoteWaveform.ts`) since this server has no audio-decoding capability, validated/bounded server-side, and echoed back for the client to attach to its `message:send` payload; `POST`/`GET /api/voice-notes` (+ `/:id`); `ChatRoom.tsx` gained a 🎤 record button (MediaRecorder) and renders received voice notes as a waveform-bar row plus an `<audio>` player
- #123 Send and view photos with self-destruct after viewing (Bumble/Snapchat) — new `SelfDestructPhotoStore` (`selfDestructPhotos.ts`) is a "view once, then gone" upload store: the sender can keep re-viewing their own sent photo indefinitely, but the first view by anyone else destroys it permanently (a `410` afterward, `404` if it never existed) — the view *is* the read, no separate consume step; `ChatMessage`/`SendMessagePayload` gain `selfDestructImageUrl`; `POST /api/self-destruct-photos` (+ `GET /:id?viewer=`, never cached); `ChatRoom.tsx` gained a 🔥📷 send button and a tap-to-reveal gate on receipt (auto-loading would consume the one-time view before the recipient chose to look) with a "photo has disappeared" placeholder once it's gone
- #124 Send stickers, emojis and GIFs (Giphy integration) (Tinder) — emojis already worked (plain unicode text needs no backend support); new `GiphyService` (`giphy.ts`) is a real third-party Giphy integration, same "unconfigured means the feature is off, not fabricated" pattern as `SpotifyService`/`InstagramService`, purely opt-in via `GIPHY_API_KEY` (never reaches the browser — the server proxies the search); `GET /api/giphy/search?q=&type=gifs|stickers`; a picked result's URL points at Giphy's own CDN, so sending one is just a regular chat image message (`imageUrl`) — no upload/storage of our own needed, unlike #122/#123's self-hosted media; new `GifPicker` search-and-grid component wired into `ChatRoom.tsx`'s composer
- #125 Show sent/delivered/read message status (Read Receipts) (Bumble) — new `ReadReceiptStore` (`readReceipts.ts`) tracks an aggregate status per message ("read" once any other participant has read it, "delivered" once any other participant's client has received it, else "sent") rather than a per-recipient list; new `message:delivered`/`message:read` socket events (author-of-the-message calls are ignored) broadcast a `message:status` update to the whole room, plus a `GET /api/rooms/:roomId/:messageId/status` REST snapshot; `ChatRoom.tsx` emits delivered the instant `message:new` arrives and read immediately if the tab is visible (or on regaining visibility for anything that arrived while hidden) — a coarser trigger than per-message viewport visibility, consistent with this app's other simplicity-over-precision calls — and renders a ✓/✓✓/blue-✓✓ indicator on the sender's own messages
- #126 Show typing indicator (Bumble) — new `TypingStore` (`typing.ts`) is a per-room set of currently-typing authors; start/stop is client-driven (the client debounces its own "stopped typing" after a 2s pause in keystrokes — there's no reliable server-side signal otherwise) via new `typing:start`/`typing:stop` socket events broadcasting `typing:update` to the room excluding the typer, plus a `GET /api/rooms/:roomId/typing` REST snapshot; a `disconnecting` handler (not `disconnect` — `socket.rooms` is already empty by then) clears a disconnected author from every room as a safety net against a stuck indicator; `ChatRoom.tsx` renders "X is typing…" above the composer
- #127 Ability to send live or text location (Bumble) — every location share carries real coordinates from the browser's Geolocation API (`ChatLocationShare` on `ChatMessage`/`SendMessagePayload`) with an optional text `label` covering the "text" half rather than a fabricated geocoding lookup; a *static* share needs no server-side tracking (just coordinates on the message, same as #122's audioUrl); new `LiveLocationShareStore` (`liveLocationShares.ts`) tracks an in-progress *live* share's position and bounds its duration (1-480 min, WhatsApp's real max) — `message:send` validates and computes the authoritative `expiresAt` server-side, and a new `location:update` socket event (rejected privately to the sharer if expired or from an impostor) broadcasts new coordinates to the room, plus a `GET /api/rooms/:roomId/messages/:messageId/location` REST snapshot; `ChatRoom.tsx` polls `getCurrentPosition` every 15s while a live share is active and renders a plain Google Maps link (no map library bundled, same "no fabricated infra" call as `SOSAlertView.tsx`'s own location link) rather than an embedded map
- #128 In-app audio call (Badoo) — new `CallStore` (`calls.ts`) is the WebRTC *signaling* state machine (ringing/active, one call per author at a time), reused unmodified by #129's upcoming video call since the mechanic is identical; new `call:invite`/`call:accept`/`call:end`/`call:signal` socket events, broadcast to the room the same way chat messages are (recipients are already joined) rather than inventing per-user socket routing, with a block check on invite and offer/answer/ICE-candidate payloads relayed opaquely — this server never touches the actual audio stream; plus `GET /api/calls/:callId` and `GET /api/calls/active/:author` REST snapshots; `ChatRoom.tsx` derives who to call from the one other author who's actually posted in the room (a group room hides the call button rather than guessing) and negotiates real peer-to-peer audio via `RTCPeerConnection` + a public STUN server — no TURN relay is configured anywhere in this app's infra, an honest, disclosed limitation for restrictive/symmetric-NAT pairs
- #129 In-app video call (WebRTC Video Call) (Badoo) — `CallStore` needed exactly one addition for this, a `video` flag on `Call` set at `initiate()`/carried through `call:invite`→`call:incoming` (the ringing/accept/end state machine and the offer/answer/ICE relay are identical to #128's audio call, which is why that store was designed to be reused rather than duplicated); `ChatRoom.tsx` gained a second "📹 Video call" button alongside #128's audio one, requests `getUserMedia({ audio: true, video: call.video })` when a call is accepted, and renders local (muted, mirrored corner) + remote `<video>` elements for a video call instead of the hidden `<audio>` element an audio call uses — same `RTCPeerConnection`/STUN-only setup and disclosed no-TURN limitation as #128
- #130 Video call locked until at least 10 text messages are exchanged (Bumble) — new `checkVideoCallEligibility()` (`videoCallGate.ts`) counts either participant's messages in the room; #128's audio call is never gated, matching Bumble's own video-specific rule — `call:invite`'s existing block check now also rejects a `video:true` invite before the threshold with a "send N more messages" reason, and a new `GET /api/rooms/:roomId/video-call-eligibility?caller=&callee=` lets the client show/disable the video-call button ahead of time instead of only finding out after a rejected click; `ChatRoom.tsx` replaces the video-call button with a "🔒 Send N more messages to unlock video calling" hint until eligible
- #131 Beauty filter and background blur during video calls (Badoo) — new `VideoCallEffectsStore` (`videoCallEffects.ts`) is a persisted per-author preference (same shape as #85's `ProfileColorThemeStore`); the actual pixel processing happens entirely client-side (`beautyFilter.ts`) when a #129 video call starts: the beauty filter draws each frame to a canvas with a real blur/brightness/saturation blend (a genuine technique production beauty filters use, not a fabricated ML model) and re-captures it as the outgoing video track, while background blur uses the browser's native `MediaStreamTrack` `backgroundBlur` constraint where actually supported — disclosed as unavailable rather than faked where it isn't, since this app bundles no ML-based person-segmentation model; `PUT`/`GET /api/video-call-effects/:author`; new toggles in `ChatRoom.tsx`
- #132 AI-suggested conversation starters (Icebreakers) (Hinge) — new `generateIcebreakers()` (`icebreakers.ts`) is genuinely personalized from this app's real shared-signal computations (#94's interest overlap, #118's music match, #119's weekend-plan match, #120's bio keyword match) rather than an invented LLM call this app has no model or API key for, same honest-heuristic scoping as #95's Smart Score; falls back to generic Hinge-style open-ended prompts to fill out the list when a pair has few or no shared signals; `GET /api/icebreakers/:author/:candidate` composes those existing per-signal endpoints' logic and respects each signal's own hide flag on both sides; new `IcebreakerSuggestions` pill-list component shown early in a conversation (fewer than 3 messages so far) that fills the composer when tapped
- #133 Ability to reply to and edit sent messages (Bumble) — "reply" already existed (swipe-to-reply, `replyToId`/`replyToAuthor`/`replyToText`, part of the original chat core); the missing half was editing, added via new `canEditMessage()` (`messageEditing.ts`): only the original sender, only within a 15-minute window (WhatsApp's real edit window, a safety/integrity bound against silently rewriting old history), and only for a plain-text message — #122/#123/#124/#127's media message types have no caption concept to edit; a new `message:edit` socket event reuses `message:send`'s own scam-content check so an edit can't slip content past moderation a fresh send would catch, and broadcasts `message:edited`; `ChatRoom.tsx` gained an inline edit box and an "(edited)" tag
- #134 Delete sent messages (Delete for Everyone) (Bumble) — new `canDeleteMessage()` (`messageDeletion.ts`) allows only the original sender, within a 24-hour window (longer than #133's 15-minute edit window, matching WhatsApp's own extended "Delete for Everyone" range — deleting a mistake is a stronger, safety-motivated action than a casual edit); unlike editing, any message type can be deleted since deletion just replaces the content fields with a placeholder rather than needing new content to render; scoped to the message record itself, not chasing down an associated stored blob in `voiceNotes.ts`/`selfDestructPhotos.ts` (already keyed by an unguessable id); new `message:delete` socket event broadcasts `message:deleted`; `ChatRoom.tsx` renders "🚫 This message was deleted" in place of the original content
- #135 First-message-by-women rule (Bumble-like Feature) (Bumble) — new `canSendFirstMessage()` (`firstMessageRule.ts`) blocks a man from sending the opening message in a woman/man match while leaving every other pairing (same-gender, non-binary/etc., or an undeclared gender) unrestricted, matching Bumble's actual behavior since it launched same-gender matching; gender itself comes from a new `GenderInfoStore` (`genderInfo.ts`), its own per-chat-author store like #67-89's other profile info rather than bridging to #21-28's auth-gated onboarding gender field; `message:send` only checks it when the client supplies an explicit `recipient` and the room has no messages yet — this app's rooms have no formal "these two people only" concept (#100's matches list still links into one shared room, not a per-match room), so `ChatRoom.tsx` accepts an optional `recipient` prop no current caller passes, ready for a future per-match room without more server work; new `PUT`/`GET /api/gender-info/:author` and a `GenderInfoEditor` on `/settings/profile`
- #136 24-hour timer to respond to the first message before the Match expires (Bumble) — new `MatchExpiryStore` (`matchExpiry.ts`) starts a 24-hour clock the moment `POST /api/swipes` creates a match, stopped the moment either side sends a message to the other (via the same `recipient` field #135 introduced) — doesn't care which side was supposed to go first, that's #135's own separate rule; `GET /api/matches/:author` drops an expired match from the list entirely (same as real Bumble removing it), and a new `GET /api/matches/:author/:candidate/expiry` powers a live countdown; `message:send` also rejects further messages to an expired match; new `MatchCountdown` "⏳ Xh Xm Xs left to say something" component on `/matches`
- #137 Extend Match chat time (Bumble) — `MatchExpiryStore` gained `extend()`: either side can push #136's 24-hour deadline back by one more full window, but only once per match, only before it expires, and only before a first message has been sent (once a conversation starts there's no deadline left to extend); new `POST /api/matches/:author/:candidate/extend`, reflected in the existing expiry endpoint's `extended` flag; `MatchCountdown` now shows an "Extend +24h" button next to the live countdown, hidden once already used
- #138 Pin important chats to the top of the list (Bumble) — new `PinnedChatsStore` (`pinnedChats.ts`), a one-sided per-viewer pin list over the other side of a match, same shape as #45's `BlockStore` (pinning from your side doesn't pin it for them); new `POST`/`DELETE /api/pinned-chats` and `GET /api/pinned-chats/:viewerAuthor`; `/matches` gained a Pin/Unpin button per row, sorts pinned matches to the top of the list, and marks them with a 📌
- #139 Archive old chats (Bumble) — new `ArchivedChatsStore` (`archivedChats.ts`), a one-sided per-viewer archive list, same shape as #138's `PinnedChatsStore`; purely a list-visibility preference, doesn't touch matching or #136/#137's expiry timer; `GET /api/matches/:author` now excludes archived matches by default and includes them (tagged `archived: true`) when called with `?includeArchived=true`; new `POST`/`DELETE /api/archived-chats` and `GET /api/archived-chats/:viewerAuthor`; `/matches` gained an Archive/Unarchive button per row and a "Show archived (N)" toggle to review and restore them
- #140 Search within conversation text (Bumble) — new `searchMessages()` (`messageSearch.ts`), a case-insensitive substring match over a room's message text, excluding #134's deleted messages (their stored text is just a placeholder) and reusing the same mutual-block filter as the plain messages list; new `GET /api/rooms/:roomId/messages/search?q=...`; `ChatRoom.tsx` gained a 🔍 Search toggle with a results list that jumps to and highlights a match using the same scroll/highlight mechanism as the existing `?m=` deep link — same disclosed limitation: jumping only works if that message is already loaded into the current page of history
- #141 Unmatch and delete chat at once (Bumble) — new `SwipeStore.unmatch()` mutually revokes an existing match (unlike #138/#139's deliberately one-sided pin/archive lists), leaving each side's prior swipe record in place so the pair can't immediately re-match by swiping again; new `DELETE /api/matches/:author/:candidate` also clears both sides' pin/archive list-state for that pair in the same call — this app's match core still has one shared chat room, not per-match DM threads (#135's disclosed scope limit), so there's no separate message thread to purge; the concrete, honest "delete chat" behavior is this instant, atomic disappearance from both viewers' `/matches` lists; new "Unmatch" button per row on `/matches` with a confirm prompt
- #143 Automatic AI warning when inappropriate/unethical content is detected (Bumble) — new `scanForInappropriateContent()` (`contentWarning.ts`) is an honest keyword/phrase heuristic for harassment and profanity, same disclosed scoping as #56's scam check/#58's spam check rather than a fabricated trained toxicity model this app has no data or infra for; unlike #56's hard block or #58's silent auto-report, this is a soft nudge — `message:send` holds the message and emits `message:warning` instead of sending it, and the sender can "Edit" (restores the text to the composer) or "Send anyway" (`overrideWarning: true` on a resend, which also auto-files a report into #41's `ReportStore` so overrides stay visible to moderation); `ChatRoom.tsx` gained the confirm banner
- #144 Automatically blur suspicious photos before the user opens them (Bumble) — new `isSenderPhotoSuspicious()` (`photoWarning.ts`) reuses #107's `fakeProfileDetector` report-count signal rather than a fabricated NSFW-image classifier this app has no vision model/API key for; `message:send` sets a new `ChatMessage.suspicious` flag on a plain `imageUrl` message (not #123's already tap-gated `selfDestructImageUrl`) from a sender with enough reports; `ChatRoom.tsx`'s new `SuspiciousPhoto` component blurs it behind a "⚠️ tap to view" warning instead of rendering it immediately, staying permanently viewable once revealed (unlike #123's destroy-on-view)
- #145 Real-time message translation for users with different languages (Bumble) — new `TranslationService` (`translation.ts`) proxies a real Google Cloud Translation API call, gated behind `TRANSLATE_API_KEY`, same "unconfigured means the feature is off, not fabricated" pattern as `giphy.ts`'s `GiphyService`/Spotify's connect flow; new `POST /api/translate` (server-only credential, never reaches the browser); on-demand per plain-text message into the viewer's own current app locale (`en`/`fa` from #9's `LocaleProvider`), not a background bulk-translate of the whole conversation; `ChatRoom.tsx` gained a 🌐 Translate toggle per message showing the translated text inline
- #146 Send invitations for real dates within the chat (Bumble) — new `createDateInvite()`/`respondToDateInvite()` (`dateInvites.ts`), a proposed real-world meetup (location, time, optional note) attached to a chat message as its own card rather than plain text; genuinely separate from #47's `SharedDateStore` (proposing a date to your match inside the conversation, not sharing an already-planned date's live status with third-party trusted contacts); `message:send` validates and attaches `ChatMessage.dateInvite` with status `"pending"`, and a new `date-invite:respond` socket event lets only the recipient accept/decline once, mutating it in place the same way `message:edit` does; `ChatRoom.tsx` gained a 📅 composer button (`DateInvitePicker`) and a `DateInviteCard` with Accept/Decline actions
- #147 Share a date proposal (suggest cinema, cafe, restaurant) (Bumble) — new `dateProposals.ts` fixed catalog (cinema/cafe/restaurant/park/drinks, labels shared via `@chatapp/shared`'s `DATE_PROPOSAL_LABELS`), a deliberately lighter-weight sibling to #146's formal `dateInvite` — no location/time/RSVP, just an instant themed conversation-starter chip closer in spirit to #132's Icebreaker suggestions (see CLAUDE.md's "Known backlog overlap: #146 and #147" for why these are two separate features, not a duplicate); `message:send` validates the category and falls back to its label as the message text; `ChatRoom.tsx` gained a 💡 composer button with the category chips, rendered as a badge-style bubble in chat
- #148 Two-player online games within chat to break the ice (Hinge) — new `ticTacToe.ts` is a genuinely playable two-player Tic-Tac-Toe (`createGame()`/`applyMove()` with real turn/win/draw validation), not a fabricated "AI game generator"; only offered against a known `recipient` (a fresh 1:1 match, same reasoning as #135's gender rule); `message:send`'s new `startGame` flag attaches `ChatMessage.game`, and a new `game:move` socket event applies one move at a time, mutating it in place the same way `message:edit` does; `ChatRoom.tsx` gained a 🎮 composer button and a `TicTacToeBoard` grid
- #149 End-to-end encrypted chat environment (optional) (Feeld) — real ECDH (P-256) + AES-GCM via the browser's native Web Crypto API (`apps/web/src/app/e2ee.ts`), not a fabricated cipher; new server-side `PublicKeyStore` (`e2eeKeys.ts`, `PUT`/`GET /api/e2ee/public-key/:author`) only ever relays each side's public key, never a private key or plaintext — `message:send` just relays the ciphertext/iv it's handed, so an encrypted message skips #56's scam check and #58's spam auto-report (the real, disclosed tradeoff genuine E2EE has); only offered against a known `recipient` (a fresh 1:1 match, same reasoning as #135/#148); disclosed simplification: the private key is stored in `localStorage` as an extractable JWK rather than a hardware-backed secure enclave, since a browser has none to target; `ChatRoom.tsx` gained a 🔓/🔒 toggle and an `EncryptedMessage` component that decrypts client-side
- #150 Save message drafts (Bumble) — new `MessageDraftStore` (`messageDrafts.ts`), a per-author-per-room preference store (same shape as `pinnedChats.ts`) that's server-persisted rather than just localStorage, so a draft survives across devices/browsers; new `PUT`/`GET /api/message-drafts/:author/:roomId`; `ChatRoom.tsx` restores a room's draft into the composer on mount, saves it debounced (same pattern as #126's typing-stop signal) as you type, and clears it the moment the message is actually sent
- #151 Push notification for a new Match (Tinder) — new `buildNewMatchNotification()` (`matchNotifications.ts`), a pure/testable notification-content function alongside `push.ts`'s real web-push I/O (same "testable pure function next to untestable I/O" shape as `messages.ts`'s `buildChatMessage`); `PushService` gained `notifyAuthor()` (targets one specific person's subscriptions, unlike `notifyOthers()`'s broadcast-to-everyone-else used by #5's message push); `POST /api/swipes` fires one to each side the moment a match is created; the existing service worker push handler from #5 already renders any `{title, body}` payload generically, so no client changes were needed
- #153 Notification for a new like (explicit or anonymous) (Tinder) — new `buildNewLikeNotification()` (`likeNotifications.ts`), deliberately generic/non-identifying content ("Someone likes you") rather than naming the liker, since a push notification can sit on a lock screen anyone nearby might glance at — the "(explicit or anonymous)" in the issue title maps to that privacy choice, not a premium-tier gate (#103's "Likes You" list already shows full identity in-app, no paywall); `PushService` gained `notifyAuthor()` (targets one specific person, unlike `notifyOthers()`'s broadcast); `POST /api/swipes` fires it for a one-sided like that didn't also create a match (a match gets #151's differently-worded notification instead, not both)
- #154 Reminder notification to respond to expiring chats (Tinder) — `MatchExpiryStore` gained `needsExpiryReminder()`/`markReminderSent()`/`getAllPairs()`, firing once per match in a 4-hour lead-time window before #136's 24-hour deadline, only while nobody's said anything yet; a new `setInterval` sweep in `server.ts` (unref'd, this app's stand-in for real job-queue/cron infra) checks every tracked match every 15 minutes and pushes both sides via `PushService.notifyAuthor()` when eligible
- #155 Notification for the start of in-app live events (Match.com) — new `LiveEventStore` (`liveEvents.ts`): any author can schedule an event (no admin role exists in this app, same scoping call as #109's squads/#110's double dates) with a title/description/start time, and others opt in via `subscribe()`; a new `setInterval` sweep in `server.ts` (unref'd, same pattern as #154's match-expiry reminder) fires the "it's starting" push to every subscriber via a new `PushService.notifyAuthors()` once the start time arrives, exactly once per event; new `POST`/`GET /api/live-events` and `POST`/`DELETE /api/live-events/:eventId/subscribe`; new `/live-events` page (linked from `/discover`) to schedule events and toggle notifications
- #156 Detailed notification category management in settings (Tinder) — new `NotificationPreferencesStore` (`notificationPreferences.ts`), one boolean per push category (newMessage/newMatch/newLike/matchExpiryReminder/liveEventStart, matching #5/#151/#153/#154/#155's push types), defaulting all to enabled; `PushService.notifyOthers()` gained an optional `recipientAllowed` filter, wired into #5's message push for the `newMessage` category — the other four categories' push send sites live on their own not-yet-merged branches and should each add their own `isEnabled()` check once merged; new `GET`/`PUT /api/notification-preferences/:author`; new `/settings/notifications` page (linked from the chat screen) with a toggle per category, keyed to the same guest chat identity push subscriptions already use
- #157 Weekly digest emails (Tinder) — new `WeeklyDigestStore`/`buildWeeklyDigest()` (`weeklyDigest.ts`): an author opts in with their own email (kept independent of #21-#25's separate account-auth email system, since that identity has no merged link to the swipe/match activity tracked under the guest chat author identity — same disclosed gap as #135's genderInfo.ts note); reports current like/match totals rather than a precise weekly delta, since `SwipeStore` doesn't timestamp individual swipes; a new hourly `setInterval` sweep (unref'd, same pattern as #154/#155) checks every subscribed author against `needsWeeklyDigest()`'s real 7-day cadence and logs the send server-side — no real email provider credentials in this environment, same stand-in as `auth.ts`'s OTP/`recovery.ts`'s recovery code; new `PUT`/`DELETE`/`GET /api/weekly-digest-email/:author`; new `/settings/weekly-digest` page (linked from the chat screen)
- #158 SMS notification for login and security alerts (Bumble) — new `SmsSecurityAlertStore` (`smsSecurityAlerts.ts`) sends a stubbed SMS (same no-real-provider pattern as `auth.ts`'s `OtpService` and `sos.ts`) on every successful sign-in across all six auth flows (phone+OTP, Google, Apple, Facebook, email recovery, WebAuthn) and on two account-security-lowering events (2FA disabled, "log out of other devices"); per-category (`login`/`accountSecurity`) opt-out preferences, on by default; no-ops gracefully for accounts with no phone number on file (OAuth/email-only sign-ins); new `GET`/`PUT /api/sms-security-alerts/preferences` and `GET /api/sms-security-alerts/history`, gated behind `requireAuth`; new `SmsSecurityAlerts` toggle-list component on `/settings/security` also shows the "sent" alert history since there's no real phone to check
- #159 In-app notification inbox (Tinder) — new `NotificationInboxStore` (`notificationInbox.ts`) records a durable per-author entry for every push-worthy event this app already sends — #151's new match, #153's new like, #154's match-expiry reminder, #155's live-event start — independent of whether the browser ever granted push permission or has a live subscription; #5's plain "new message" push is deliberately not mirrored here since that event is already visible in the chat list itself; new `GET /api/notification-inbox/:author`, `POST /api/notification-inbox/:author/read-all`, `POST /api/notification-inbox/:author/:id/read`; new `NotificationInbox` 🔔 bell-icon component in the chat header shows an unread-count badge and a dropdown list, marking everything read the moment it's opened
- #160 Custom ringtone and vibration for app notifications (Tinder) — new `NotificationSoundStore` (`notificationSound.ts`) is honestly split by what the web platform actually supports: the Push API has no cross-browser "custom sound" option in any browser, so "ringtone" is a real, synthesized-in-browser (Web Audio oscillator, no bundled audio asset) tone that only plays while the tab is foreground (see `apps/web/src/app/notificationSound.ts`, wired into #159's polling `NotificationInbox.tsx` so it fires independent of push permission); "vibration" is the genuinely real half — turning it off is honored by the actual push payload server.ts sends for #151/#153/#154's single-recipient sends, read by `sw.js`'s `showNotification()` as a real background `vibrate` pattern even when the app isn't focused (broadcast sends like #5/#155 aren't per-recipient customized, disclosed rather than silently ignored); new `GET`/`PUT /api/notification-sound/:author`; new `/settings/notification-sound` page (linked from the chat screen) with a ringtone picker + live preview and a vibration toggle
- #161 Settings for how online/offline status is displayed (Tinder) — new `PresenceVisibilityStore` (`presenceVisibility.ts`) layers WhatsApp/Bumble's real online-status/last-seen privacy pair on top of #110's `PresenceStore`, which still tracks the real connection state underneath; `GET /api/presence/:author` applies it at read time, and an optional `?viewer=` enforces the genuine mutual "hide yours, lose theirs" gating WhatsApp's actual Last Seen setting has — a viewer who turned off their own last-active also can't see the author's, not just a one-way toggle; new `GET`/`PUT /api/presence-visibility/:author`; new `/settings/presence-visibility` page (linked from the chat screen) with the two toggles
- #163 Set measurement units (cm/inch, km/miles) (Tinder) — new `MeasurementUnitsStore` (`measurementUnits.ts`) is a per-viewer display preference over the one measurement this app actually surfaces to users: height (from #67's `heightInfo.ts`), which keeps being stored in cm regardless — distance (km/miles) has no user-facing display anywhere yet (`crossedPaths.ts`'s `haversineDistanceKm` is an internal threshold check, never shown to a user), so it's out of scope rather than adding a distance-away UI this issue didn't ask for; new `GET`/`PUT /api/measurement-units/:author`; `HeightInfoEditor.tsx` switches between a cm input and a feet/inches input based on the signed-in author's own preference (converting through cm on save), and `profile/[author]/page.tsx` formats a viewed profile's height using the *viewer's* own preference; new `/settings/measurement-units` page (linked from the chat screen)
- #164 Hide profile without deleting the account (Snooze Account) (Bumble) — new `SnoozeAccountStore` (`snoozeAccount.ts`) is a genuinely distinct pause from #111's Vanish Mode: snoozed hides an author from *everyone's* discovery pool with no "already liked me" exception (unlike Vanish Mode's), and also blocks the snoozed author from swiping on new candidates themselves via `POST /api/swipes` — but deliberately never touches matches or messages, so existing chats stay fully reachable while snoozed, honoring "without deleting the account"; new `GET`/`PUT /api/snooze-account/:author`; new `SnoozeAccountEditor` toggle on `/settings/profile`, next to `VanishModeEditor`
- #166 Manage access permissions (location, camera, microphone access) (Tinder) — new `PermissionsSettings.tsx` genuinely queries the browser's real Permissions API (`navigator.permissions.query`) for each of location/camera/microphone and lets the user trigger the actual `getUserMedia`/`geolocation.getCurrentPosition` prompt via a "Request access" button; honestly discloses that no JavaScript API can *revoke* a permission the browser already granted — only the browser's own site settings can, so that's what's shown instead of a fake "revoke" button; each observed status is mirrored (not authoritatively determined) server-side by new `PermissionsStatusStore` (`permissionsStatus.ts`) via `GET`/`PUT /api/permissions-status/:author`; new `/settings/permissions` page (linked from the chat screen)
- #167 Clear app cache (Feeld) — new `clearAppCache()` (`apps/web/src/app/clearCache.ts`) genuinely clears this PWA's real service-worker Cache Storage (`sw.js`'s `CACHE_NAME` — cached app-shell assets and the offline page) via `caches.keys()`/`caches.delete()`; deliberately leaves `localStorage` untouched since guest identity/theme/locale/data-saver are real settings, not cache, and the browser's own HTTP cache has no JS API to clear at all (both disclosed rather than faked); new `CacheClearLogStore` (`cacheClearLog.ts`) is a "client reports, server records" audit log (same shape as #166's `PermissionsStatusStore`) behind `GET`/`POST /api/cache-clear-log/:author`, powering a "Last cleared: …" line; new `/settings/clear-cache` page (linked from the chat screen)
- #169 Direct links to terms, privacy policy and support (Feeld) — new real, substantive `/terms`, `/privacy-policy`, and `/support` pages describing what this app actually does (guest/OAuth identity, matches, messages, media, location sharing) and pointing at its real existing self-serve tools (Safety Center, active sessions, data export, permissions) rather than generic boilerplate or an invented support-ticket backend; new `TermsAcceptanceStore` (`termsAcceptance.ts`) records that an author has viewed the current terms version the moment they open `/terms` — this passwordless app has no account-creation gate to require explicit consent at, so viewing is the honest equivalent — via `GET`/`POST /api/terms-acceptance/:author`; new `/settings/legal` hub page (linked from the chat screen) with the three direct links this issue asks for
- #170 Option to log out of the account from all devices (Tinder) — new `TokenService.revokeAllSessions()` (`apps/api/src/auth.ts`) is a genuinely distinct action from #60's "log out of other devices": it revokes the caller's own current session too, not just everyone else's, so the whole account signs out with no exception (the already-issued access token keeps working until its natural 15-minute expiry — same disclosed JWT-revocation trade-off #60 already has — but the session list it reads comes back empty); new `DELETE /api/auth/sessions/all`, registered ahead of the `:sessionId` route the same way `/others` already is; `/settings/sessions` gained a "Log out of all devices" button that clears the local stored session on success since there's nothing left to keep signed in with
- #171 Comprehensive admin dashboard with analytical charts (Tinder) — first issue in the "Admin Panel & Moderation" category; new `adminMetrics.ts` is honestly scoped to what's real: every number is a genuine live count pulled from this app's own stores (`UserStore.getTotalCount()`, `SwipeStore.getTotalMatchCount()`, `messagesByRoom` totals, `ReportStore.getTotalCount()`, `BlockStore.getTotalBlockCount()`) rather than fabricated sample data, and the implementation guide's "fine-grained RBAC and full audit logging"/ML pre-screening are production-scale infra this environment has no role system or ML model to build — gated instead behind one shared `ADMIN_API_KEY` env var (`x-admin-key` header), 503 when unconfigured rather than silently open, same fail-closed pattern as Google/Apple Sign-In's own credential gates; new `GET /api/admin/metrics`; new `/admin` page with stat tiles and a bar chart (built with the dataviz skill's validated CVD-safe categorical palette, new `--chart-series-1..5` tokens in `globals.css`) comparing the five totals
- #172 Smart and manual review of uploaded photos (Bumble) — new `PhotoReviewStore` (`photoReview.ts`), built on #171's admin-key gate (new `requireAdmin()` helper in `server.ts`, reused by both issues' routes); "smart" reuses the same real report-count signal #107's `fakeProfileDetector`/#144's `photoWarning.ts` already use to auto-prioritize the queue by severity, rather than a fabricated ML image classifier; "manual" is the actual admin approve/reject decision, and rejecting a photo has a real effect — it's removed from the owner's album via `PhotoAlbumStore.removePhoto()`, not just flagged; every upload (`POST /api/photos`) now joins the queue automatically; new `GET /api/admin/photo-review-queue` and `POST /api/admin/photo-review/:photoId`; new `/admin/photos` page (linked from `/admin`) — deliberately doesn't render a photo thumbnail, since `GET /api/photos/:id` enforces #59's per-album view-access level that an arbitrary admin viewer wouldn't satisfy, a disclosed gap rather than a faked preview
- #173 Reported users management (Reported Users Queue) (Bumble) — `ReportStore` (`reports.ts`) — until now "internal-only... not wired to any HTTP route" per its own doc comment — finally gets the authenticated admin surface that comment called for, now that #171/#172 established the admin-key gate; new `getReportedUsersQueue()` groups still-pending reports by reported author (not a flat per-report list) sorted by pending-count highest first, the same real severity-based prioritization #172's photo queue uses; new `review()` records an admin's actual resolved/dismissed decision with a reviewer, timestamp, and optional note; new `GET /api/admin/reported-users` and `POST /api/admin/reports/:reportId/review`; new `/admin/reports` page (linked from `/admin`) showing each flagged user's pending reports with Resolve/Dismiss actions
- #174 Admin system to approve verification badges (Raya) — closes the gap `verification.ts`'s own doc comment flagged ("a real deployment would still need a moderation/review pipeline before treating 'verified' as a trust signal"): `VerificationStore` now tracks a real `pending`/`approved`/`rejected` status per submission instead of auto-verifying on upload, so `isVerified()` (and therefore the profile badge and any `requireVerifiedOnly` discovery filter) only turns true once an admin has actually approved it; new `getPendingQueue()` (oldest-first, same admin-key gate as #171-173) and `review()`; new `GET /api/admin/verification-queue`, `POST /api/admin/verification-queue/:userId/review`, and an admin-key-gated `GET /api/admin/verification-queue/:userId/selfie` that extends — not violates — the "selfie never served back" privacy invariant by scoping raw-image access strictly to this one authenticated review workflow; new `/admin/verification` page (linked from `/admin`) fetches the selfie as a blob with the `x-admin-key` header (an `<img src>` can't carry custom headers) and renders it via `URL.createObjectURL`; `LiveSelfieCapture.tsx` copy updated to say the badge appears after admin review, since verification is no longer instant
- #175 Ability to permanently or temporarily ban offending users (Ban / Shadowban) (Bumble) — new `BanStore` (`bans.ts`), gated by the same admin-key check as #171-174; a "banned" account (temporary with an expiry, or permanent) is hard-blocked from swiping (`POST /api/swipes` → 403) and sending messages (`message:send` → `message:rejected` with reason `banned`) and disappears from discovery, a deliberately visible consequence; a "shadowbanned" account keeps swiping and messaging normally with no error — it only vanishes from other people's `/api/swipe-candidates` pool, the same discovery-layer-only mechanism #111's Vanish Mode and #164's Snooze Mode already use, since this app has no per-conversation delivery pipeline to selectively suppress without changing message broadcast for every user (an honestly-scoped boundary, not a fabricated one); new `GET /api/admin/bans`, `POST /api/admin/bans/:userId`, `DELETE /api/admin/bans/:userId`; new `/admin/bans` page (linked from `/admin`) to issue and lift bans/shadowbans
- #176 Automatic filtering system for inappropriate words and profanity (Bumble) — new `profanityFilter.ts` reuses #143's `contentWarning.ts` word list (now exported) but is a genuinely different mechanism: silent, automatic masking (`fuck` → `f***`) rather than #143's interactive "are you sure?" chat warning, so it's applied where no such flow already exists — `BioStore.update()` and `DisplayNameModeStore.update()`'s nickname field — rather than double-handling the same chat text two conflicting ways; backend-only, since the existing bio/nickname edit UIs already just render whatever the store returns
- #177 Manage financial plans, pricing and discount codes (Bumble) — new `PricingPlanStore` (`pricingPlans.ts`) and `DiscountCodeStore` (`discountCodes.ts`), admin-key gated like #171-176; this app has no real payment processor (same disclosed gap as #105/#106's free Boost — "no premium tier to gate it behind"), so a plan is genuine, persisted admin-curated pricing data rather than a live billing product, and redeeming a discount code validates it and records a real redemption count rather than faking an "applied to your order" confirmation; new `GET/POST /api/admin/pricing-plans`, `PUT`/`DELETE /api/admin/pricing-plans/:planId`, `GET/POST /api/admin/discount-codes`, `DELETE /api/admin/discount-codes/:code`; new public `GET /api/pricing-plans` and `POST /api/discount-codes/redeem`; new `/admin/pricing` management page (linked from `/admin`) and a public `/pricing` page
- #178 Send broadcast messages and notifications (Bumble) — new admin route reuses `PushService` (new `getSubscribedAuthors()`) and `NotificationPreferencesStore` (new `adminBroadcast` category, on by default) to reach every currently push-subscribed, opted-in author — this app has no separate email/SMS channel, so that's the honest reach of "everyone"; each recipient also gets a #159 in-app inbox entry via `NotificationInboxStore` regardless of push permission; new `BroadcastStore` (`broadcasts.ts`) is the audit-trail half, recording who sent what to how many people and when; new `POST/GET /api/admin/broadcasts`; new `/admin/broadcasts` page (linked from `/admin`) to compose a broadcast and see send history; `/settings/notifications` gets a new "Announcements" toggle for the category
- #179 Tools to analyze user behavior and conversion rate (Bumble) — new `analyticsFunnel.ts` computes a real signup → started-onboarding → completed-onboarding → made-a-swipe → got-a-match → sent-a-message funnel with genuine conversion percentages, from new counting methods on `OnboardingStore` (`getStartedCount()`/`getCompletedCount()`), `SwipeStore` (`getSwiperCount()`/`getMatchedAuthorCount()`), and a real 24h "daily active users" figure from a new `PresenceStore.getActiveWithinCount()` — same "real numbers from this app's own stores, not a fabricated ML behavior model" honesty as #171's `adminMetrics.ts`; new `GET /api/admin/analytics/funnel`; new `/admin/analytics` page (linked from `/admin`, built with the dataviz skill) rendering the funnel as a single-hue horizontal bar chart with a table-view toggle
- #180 Live admin support for users via ticket or chat (Bumble) — new `SupportTicketStore` (`supportTickets.ts`): a real asynchronous ticket thread rather than a live-staffed chat, since this app has no real support team behind it — an admin reply moves an open ticket to "in progress", the user and admin can keep replying, and an admin marks it resolved; new `POST /api/support/tickets`, `GET /api/support/tickets/:author[/:ticketId]`, `POST /api/support/tickets/:author/:ticketId/reply`, and admin-key-gated `GET /api/admin/support-tickets`, `POST /api/admin/support-tickets/:ticketId/reply`, `PUT .../status`; the existing `/support` page (#169's self-serve links) gets its old static "email us" replaced with this real ticket flow as the "Contact us" section — the self-serve links above it (Safety Center, sessions, data export, permissions) are unchanged; new `/admin/support` page (linked from `/admin`) for the admin side of the queue
- #181 Smart detection of multiple accounts created from one device (Tinder) — completes #53's `DuplicateAccountStore`, whose own doc comment (`DuplicateAccountNotice.tsx`) flagged the gap: "no moderator/review UI in this app yet." New `getFlaggedClusters()` groups by the raw shared-IP/shared-device-fingerprint hash bucket (rather than #53's per-user pairwise `getStatus()`) so an admin sees each cluster once, sorted largest-first — the same real "bigger group is a stronger signal" severity #172/#173's report-count sort already uses; neither the raw IP nor fingerprint is ever exposed, only the linked account IDs, same privacy stance as #53; new admin-key-gated `GET /api/admin/duplicate-accounts`; new `/admin/duplicate-accounts` page (linked from `/admin`)
- #182 Full control over Explore section content and hashtags (Bumble) — turns #99's Explore Mode from a fixed, code-defined theme catalog (cafes/sports/travel constants) into a real admin-managed one: new `ExploreThemeStore` (`exploreThemes.ts`), gated the same admin-key way as #171-181; "hashtags" means #79's existing fixed interest-tag catalog (`INTEREST_CATALOG`) — a theme's content is which of those real tags it's built from, the same "backend-configurable within a fixed catalog" scoping call #79 itself already made, not a fabricated open-ended tagging engine; `ExploreModeStore` now validates a swiper's selection against a real, currently-active theme instead of a fixed enum, seeded at startup with the original three themes so existing behavior is unchanged until an admin edits them; `GET /api/explore-mode/catalog` now returns whatever themes are active; new admin-key-gated `GET/POST /api/admin/explore-themes`, `PUT`/`DELETE /api/admin/explore-themes/:themeId`; new `/admin/explore-themes` page (linked from `/admin`); `ExploreModeSelector.tsx` now renders real admin-set theme names instead of a hardcoded emoji/label map
- #183 Set default geographic boundaries and discovery radii (Bumble) — new `DiscoveryBoundariesStore` (`discoveryBoundaries.ts`): a single platform-wide min/max/default search radius, gated the same admin-key way as #171-182, that onboarding.ts's `searchRadius` step now actually validates against instead of fixed `MIN_SEARCH_RADIUS_KM`/`MAX_SEARCH_RADIUS_KM` constants (still the built-in starting values, 1-160km/25km default, so existing behavior is unchanged until an admin edits them); new public `GET /api/discovery-boundaries` and admin-key-gated `PUT /api/admin/discovery-boundaries`; new `/admin/discovery-boundaries` page (linked from `/admin`); the onboarding radius picker now fetches the current bounds from the server instead of hardcoding them client-side
- #184 Manage domains and website access (Bumble) — new `AllowedDomainStore` (`allowedDomains.ts`): a real, admin-adjustable CORS allowlist gated the same admin-key way as #171-183, enforced in `server.ts`'s `cors()` origin check; an empty allowlist (the default) preserves the previous allow-every-origin behavior, so adding the first entry is what starts restricting access; new admin-key-gated `GET/POST /api/admin/allowed-domains`, `DELETE /api/admin/allowed-domains/:origin`; new `/admin/allowed-domains` page (linked from `/admin`)
- #185 Detailed logging of all financial transactions and refunds (Tinder) — new `TransactionLogStore` (`transactionLog.ts`): this app has no integrated payment processor yet (#191-196 are still open, same disclosed gap as pricingPlans.ts's "no premium tier to gate it behind"), so it's a real admin-facing manual ledger — an admin logs a purchase actually processed out-of-band and can issue a refund against it, which marks the original and appends a linked negative-amount refund entry — rather than a fabricated auto-logging hook with nothing real to log yet; gated the same admin-key way as #171-184; new admin-key-gated `GET/POST /api/admin/transactions`, `POST /api/admin/transactions/:id/refund`; new `/admin/transactions` page (linked from `/admin`)
- #186 View server health status and active sockets (Bumble) — new admin-key-gated `GET /api/admin/server-health`, defined in `createChatServer()` (not `createApp()`, since it needs `io`) and reachable via a new `requireAdmin` returned from `createApp()`: genuine `process.uptime()`/`process.memoryUsage()` plus a live Socket.io connection count from `io.engine.clientsCount` rather than a separately-tracked counter that could drift; new `/admin/server-health` page (linked from `/admin`)
- #187 Manage admin access roles (RBAC) (Bumble) — new `AdminRoleStore` (`adminRoles.ts`): the actual multi-tier RBAC #171's `adminMetrics.ts` disclosed this app didn't have yet ("a single shared secret... not a real multi-tier RBAC system"). A named admin account gets its own generated API key and one fixed role (superadmin/moderator/support/finance); the pre-existing `ADMIN_API_KEY` env var still works unchanged as the implicit, always-superadmin bootstrap key, so none of the existing admin routes regress. New `requireRole` gate demonstrated on its own CRUD (`GET/POST /api/admin/roles`, `DELETE /api/admin/roles/:id`): list is readable by superadmin or moderator, create/revoke need superadmin — retrofitting every pre-existing admin route to a specific non-superadmin role is future, route-by-route work; new `/admin/roles` page (linked from `/admin`)
- #188 A/B testing support for algorithm changes (Hinge) — new `ExperimentStore` (`experiments.ts`): a named experiment's variants, a deterministic per-user bucket (sha256 of experimentId+author, stable across calls, not re-rolled each request), and genuine assignment counts read back from those real assignments; no algorithm is wired to branch on the returned variant yet — that's separate, feature-specific follow-up for whichever algorithm an experiment targets. Admin-key-gated `GET/POST /api/admin/experiments`, `PUT /api/admin/experiments/:id` (activate/deactivate), `GET /api/admin/experiments/:id/stats`; public `GET /api/experiments/:id/assignment?author=` so any part of the app can resolve a user's own bucket; new `/admin/experiments` page (linked from `/admin`)
- #189 View user retention rate (Bumble) — new `retention.ts`'s `computeRetention()`: signup time comes from `UserStore.createdAt` and "returned" comes from `TokenService`'s per-session `lastUsedAt` (new `listCreatedTimestamps()`/`getLastUsedAt()` on those two stores) — the one place in this app where signup and later activity share the same authenticated `userId` identity, unlike the self-reported chat `author` string `PresenceStore`/`SwipeStore` use elsewhere, so it's the one place retention can actually be computed rather than guessed at across two identity spaces that don't correspond. Day-1/7/30 cohort windows, each only counting users old enough to qualify as eligible; admin-key-gated `GET /api/admin/retention`; new `/admin/retention` page (linked from `/admin`)
- #190 Export reports in CSV and PDF format (Bumble) — new `reportExport.ts` (`reportsToCsv()`/`reportsToPdf()`, the latter via the new `pdfkit` dependency for a real generated PDF, not a fabricated one): a real downloadable export of every moderation report #41/#172/#173 already collect (new `ReportStore.listAll()`), for an admin's own record-keeping/compliance rather than only viewable in the review queue. Admin-key-gated `GET /api/admin/reports/export.csv` and `.../export.pdf`; the existing `/admin/reports` page (#173's reported-users queue) gets two new Export CSV/PDF buttons
- #191 Premium subscription plans (Gold, Platinum, VIP) (Tinder) — new `SubscriptionStore` (`subscriptions.ts`): real recurring billing via RevenueCat/Play Billing/Apple IAP needs a payment processor and app-store accounts this environment doesn't have (same disclosed gap as `pricingPlans.ts` and #105/#106's free Boost), so this is three fixed, named tiers (unlike `PricingPlanStore`'s admin-configurable generic plans, these match Tinder's actual product names) with a genuine per-author entitlement — a real 30-day `subscribedAt`/`expiresAt` checked against wall-clock time, self-service subscribe/cancel since there's nothing to charge yet. Wiring a specific perk to require a tier is separate, feature-specific follow-up. Public `GET/POST/DELETE /api/subscriptions/:author`; admin-key-gated `GET /api/admin/subscriptions`; the `/pricing` page (#177) gets a new Premium section to subscribe/cancel; new `/admin/subscriptions` page (linked from `/admin`)
- #192 In-app credit/bank payment gateway (Raya) — new `PaymentMethodStore` (`paymentMethods.ts`): a real charge network call (Stripe, Plaid, etc.) needs a payment processor this environment has no credentials for (same disclosed gap as #191); what's real and safe to build without one is a payment-method-on-file record holding only a brand and last 4 digits — the same shape even a real Stripe Elements/Plaid Link integration would return to a server, since neither ever hands a server the full card/account number. Public `GET/POST/DELETE /api/payment-methods/:author[/:methodId]`, `PUT .../:methodId/default`; new `/settings/payment-methods` page (linked from `/pricing` and `/support`)
- #193 Google Play Billing in-app payment (Tinder) — new `GooglePlayBillingBridge` (`googlePlayBilling.ts`): this repo has no native Android app to actually drive a Play Billing purchase or a live Play Console subscription to receive real notifications from — not a "web equivalent" situation like #5's Web Push, a genuinely platform-specific gap. What's real: the server-side bridge any Play Billing integration needs regardless of platform — linking the purchaseToken a client gets back immediately after purchase to this app's author identity and #191's `SubscriptionStore`, then staying in sync via Google's real Real-time Developer Notification type codes (a real base64 Pub/Sub envelope decoder, `parseGooglePlayRtdn()`); verifying the webhook's signing JWT needs a live Pub/Sub push subscription this environment doesn't have, so that's the one piece left undone. New `POST /api/subscriptions/google-play/link` and `POST /api/subscriptions/webhooks/google-play` — no web UI, since there's no web equivalent of this feature to build
- #194 Apple In-App Purchase (Tinder) — new `AppleAppStoreBridge` (`appleAppStore.ts`), the iOS counterpart to #193 with the same disclosed gap (no native iOS app to drive a StoreKit purchase or receive real Server Notifications from). Decodes Apple's real nested-JWS App Store Server Notifications V2 shape (an outer signed payload carrying a `signedTransactionInfo` that's itself a signed JWS) without verifying either signature — that needs Apple's live certificate chain — then links the resulting `originalTransactionId` to this app's author identity and #191's `SubscriptionStore`, staying in sync via Apple's real `notificationType` values (`SUBSCRIBED`/`DID_RENEW` activate, `EXPIRED`/`REFUND`/`REVOKE` cancel). New `POST /api/subscriptions/apple/link` and `POST /api/subscriptions/webhooks/apple` — no web UI, since there's no web equivalent of this feature to build
- #195 Cryptocurrency payment (Tinder) — new `CryptoChargeStore` (`cryptoPayments.ts`): a real gateway (Coinbase Commerce, BitPay, etc.) generating a live deposit address at a real-time exchange rate needs a merchant account this environment has no credentials for (same disclosed gap as #192), but unlike #193/#194's mobile IAP webhooks (which can't verify their signature at all without live platform certificates), Coinbase Commerce's webhook signature is a self-contained HMAC-SHA256 check this app genuinely performs and proves correct (`verifyCoinbaseWebhookSignature()`, gated fail-closed the same way as `ADMIN_API_KEY`) — computed over the exact raw request body via a new `verify` callback on `server.ts`'s `express.json()`. A confirmed charge logs a real entry in #185's `TransactionLogStore`. New `POST /api/payments/crypto/charges`, `GET /api/payments/crypto/charges/:id`, `POST /api/payments/crypto/webhooks/coinbase`
- #196 Purchase in-app coin/token packages (Coffee Meets Bagel) — new `CoinStore` (`coins.ts`): a real per-author coin balance backing a fixed, curated 3-package catalog (matching the reference app's own "quality over quantity" philosophy, not an arbitrary top-up amount); "Buy" credits the balance immediately since there's no payment processor to charge against yet (same disclosed gap as #191/#192). `spend()` is real, general-purpose debit infrastructure with an insufficient-balance check — #197's "send virtual gifts using coins" is the first feature to actually call it. New `GET /api/coins/packages`, `GET /api/coins/:author`, `POST /api/coins/:author/purchase`, `POST /api/coins/:author/spend`; new `/coins` page (linked from `/support`)
- #197 Ability to send virtual gifts in chat using coins (Coffee Meets Bagel) — a real chat message type, the same "client sends a catalog id, server fills in the authoritative details" shape #146's dateInvite and #147's dateProposalCategory already use: new `GIFT_CATALOG` (`@chatapp/shared`, 3 fixed gifts) and a `gift`/`giftId` field on `ChatMessage`/`SendMessagePayload`. `message:send` looks up the real cost, debits it via #196's `CoinStore.spend()`, and rejects with `invalid_gift`/`insufficient_coins` before a client can ever forge a gift it didn't pay for. New 🎁 picker button in `ChatRoom.tsx` showing the sender's real coin balance
- #198 Purchase a single Boost or Boost package (Tinder) — #105/#106's original free Boost/Super Boost (`profileBoost.ts`) is left exactly as it was, since other code already depends on it working unconditionally; this adds the real purchasable alternative real Tinder also has alongside its occasional free/promo boosts. New `BOOST_PACKAGES` (a single boost or a discounted 5-pack, priced in #196's coins), `ProfileBoostStore.grantCredits()`/`activateBoostWithCredit()` (reuses the exact same 30-minute activation mechanics as the free path). New `GET /api/profile-boost/packages`, `GET /api/profile-boost/:author/credits`, `POST /api/profile-boost/:author/purchase`, `POST /api/profile-boost/:author/activate-with-credit`; `ProfileBoost.tsx` gets a new "buy a package" section alongside its existing free buttons
- #199 Purchase a separate Super Like package (Tinder) — #93's real free daily Super Like allowance (`DAILY_SUPER_LIKE_LIMIT` in `swipes.ts`) is untouched; this adds a purchasable credit (a single Super Like or a discounted 5-pack, priced in #196's coins, same shape as #198's `BOOST_PACKAGES`) that `recordSwipe()` falls back to once the free daily allowance is used up, rather than rejecting outright. `undoLastSwipe()` correctly refunds a purchased credit (not the daily allowance) when that's what funded the Super Like being undone, and vice versa. New `GET /api/super-likes/packages`, `GET /api/super-likes/:author/credits`, `POST /api/super-likes/:author/purchase`; `discover/page.tsx` shows purchased credits alongside the free count and offers packages to buy once the free ones run out
- #200 Ability to see who liked you as a paid feature (Tinder) — closes the gap #103's `getLikedBy()` originally disclosed ("this app has no premium tier to gate it behind"), now that #191 provides one: `GET /api/liked-you/:author` still returns the real count for everyone (real Tinder's own "blurred grid + count" pattern) but only includes the actual identities once `SubscriptionStore.getStatus()` shows an active tier — `swipes.ts`'s `getLikedBy()` itself is untouched and stays free of any subscription-store dependency, with the gating applied at the route. `/liked-you` shows a blurred count and a "Subscribe" link to `/pricing` for a non-subscriber, the real list for a subscriber
- #201 Ability to remove ads for pro users (Tinder) — a real ad network (AdMob, Meta Audience Network, etc.) needs a publisher account this environment has no credentials for, same disclosed gap as Google/Apple Sign-In's own credential requirement elsewhere; new `AdBanner.tsx` renders an honestly-labeled placeholder slot rather than pretending to serve a real ad. What's real: whether the slot renders at all — new `GET /api/ads/:author/should-show` (a dedicated decision, not the client re-deriving it from subscription state itself) returns `false` once any active #191 subscription tier is present; `AdBanner` is placed on `/discover`, matching Tinder's real between-cards ad placement
- #202 Free trial for a few days of subscription (Tinder) — real Tinder's trial auto-converts to a real paid period via its billing provider on expiry; this app has none, so `SubscriptionStore.startTrial()`'s real 3-day trial just lapses like any other subscription (`isTrial` distinguishes it) rather than auto-charging — a user still has to actually subscribe afterward. One-time-only per author (tracked independently of the subscription record, so cancelling doesn't reset eligibility), the real anti-abuse rule Tinder also enforces. New `GET /api/subscriptions/:author/trial-eligible`, `POST /api/subscriptions/:author/trial`; `/pricing` gets a "Start 3-day free trial" button per tier for an eligible user
- #203 Discount code and upgrade coupon system (Tinder) — the "upgrade coupon" half, distinct from #177's `DiscountCodeStore` price discounts (which still have no checkout to apply to): new `UpgradeCouponStore` (`upgradeCoupons.ts`) validates a redemption and a new `SubscriptionStore.grantDays()` applies the genuinely real effect — a real #191 subscription tier for the coupon's own admin-configured number of days, distinct from `subscribe()`'s fixed 30 days or `startTrial()`'s fixed 3. One redemption per author per coupon, the same anti-abuse rule #202's trial enforces. New admin-key-gated `GET/POST /api/admin/upgrade-coupons`, `DELETE /api/admin/upgrade-coupons/:code`, public `POST /api/upgrade-coupons/redeem`; `/admin/pricing` gets a coupon management section, `/pricing` a redeem field
- #204 Referral system to invite friends for free subscription days (Tinder) — new `ReferralStore` (`referrals.ts`): a real, unique per-author code (one redemption per referee ever, no self-redemption); redeeming one credits real #191 subscription days to both the referrer and the new referee via a new `SubscriptionStore.extendOrGrant()`, which extends an already-active subscription's expiry (keeping its current tier) rather than replacing it the way `grantDays()`/`subscribe()` do — a referral reward should never shorten or downgrade what a user already has. New `GET /api/referrals/:author/code`, `POST /api/referrals/redeem`; `/pricing` gets an "Invite friends" section showing the user's code, real referral count, and a redeem field
- #205 Seasonal and occasion-based discounts (Valentine's, Black Friday) (Tinder) — new `SeasonalDiscountStore` (`seasonalDiscounts.ts`): unlike #177's admin-managed `DiscountCodeStore`, a fixed calendar of named campaigns (Valentine's Day Feb 10-16, Black Friday Nov 24-30) that activate/deactivate purely from the real wall-clock month/day, recurring every year with zero admin upkeep; `redeem()` only succeeds while a campaign is genuinely active right now and tracks real per-campaign redemption counts, the same honest "no checkout to apply a price cut to" stand-in #177/#203 already use. New public `GET /api/seasonal-discounts/active`, `POST /api/seasonal-discounts/redeem`; `/pricing` shows a banner only while a campaign is currently open
- #206 Auto-renewable subscriptions (Tinder) — a real self-service `subscribe()` now defaults to `autoRenew: true` (same as real Tinder); on expiry, `getStatus()` actually rolls the subscription into another full period at the same tier instead of deleting it (rolling forward through every period missed, not just one, so a long-unchecked subscription never desyncs) — there's still no real recurring charge behind it, same disclosed payment-processor gap `subscribe()` itself already has. A trial, coupon grant, or referral reward stays a one-time perk (`autoRenew: false`), preserving #202's already-shipped trial behavior exactly. New `SubscriptionStore.setAutoRenew()` and `PUT /api/subscriptions/:author/auto-renew` (turning it off doesn't end access early, only skips the *next* renewal); `/pricing` gets an auto-renew checkbox on an active (non-trial) subscription
- #207 Manage subscription cancellation from within the app (Tinder) — new `SubscriptionStore.cancelAtPeriodEnd()`/`POST /api/subscriptions/:author/cancel`: the real cancellation behavior every actual subscription platform (App Store, Play Store, a web billing portal) has — turns off #206's auto-renew while keeping access until the period already paid for genuinely ends, distinct from the pre-existing `DELETE /api/subscriptions/:author` (an immediate hard removal, now repositioned as an admin/support-style action rather than the primary user flow). `/pricing`'s "Cancel subscription" button now calls the graceful path and shows "Cancelled — you'll keep access until…" with a "Resume subscription" undo option before the period ends, plus a separate, less prominent "End access immediately" for the hard-removal path
- #208 Pay to open a direct chat without needing a Match (Tinder) — this app's chat is a single shared room with no formal per-pair match-gating to retrofit a paid bypass around without breaking #135/#136's already-shipped first-message rules (see directMessageRequests.ts), so the real, additive feature is a request-based channel modeled on Tinder's own "Message Before Match": new `DirectMessageRequestStore` spends 100 of #196's coins (validated before spending, never charging for a request that was going to be rejected anyway) to send one message that lands in the recipient's own inbox rather than the open room, and only becomes an ordinary conversation once they explicitly accept it. New `POST /api/direct-message-requests`, `GET /api/direct-message-requests/:author/inbox`, `POST /api/direct-message-requests/:id/respond`; a "Send a direct message" form on `/profile/[author]` and a new `/message-requests` inbox page (linked from `/discover`)
- #209 Purchase an unlimited Super Rewind package (Tinder) — #92's original Rewind was free with no daily cap at all (only ever limited to the single most recent swipe), same "no premium tier to gate it behind" honest scoping #105/#106's Boost doc comments disclosed for themselves; this closes that gap with a real `DAILY_REWIND_LIMIT` (1/day) and a real, coin-priced, time-bounded (24h) removal of it — priced as "unlimited" rather than an N-pack of credits, matching this issue's own framing unlike #198/#199's packages. New `GET /api/rewinds-remaining/:author`, `POST /api/rewinds/:author/purchase-unlimited`; `/discover`'s Rewind button disables once the free daily Rewind is used, with a "Get unlimited Rewinds (150 coins)" purchase option
- #210 Ability to gift a subscription to other users (Coffee Meets Bagel) — a fixed, one-per-tier 7-day `GIFT_PACKAGES` catalog priced in #196's coins; reuses #204's `SubscriptionStore.extendOrGrant()` rather than a new grant primitive, since "extend if the recipient already has an active subscription (keeping their tier), else grant fresh at the gifted tier" is already exactly the right gifting semantics. Self-gifting and an unknown package are rejected before any coins are spent. New `GET /api/subscriptions/gift-packages`, `POST /api/subscriptions/gift`; a "Gift a subscription" section on `/pricing` lets the signed-in author spend coins to gift a friend by name
- #211 Daily Spin wheel to earn free coins (Coffee Meets Bagel) — new `DailySpinStore` (`dailySpin.ts`): a real weighted-random prize wheel (small, common payouts and a rare jackpot) with one spin per UTC day per author, paid out through #196's `CoinStore.credit()` (a new method for earning coins outright rather than purchasing a package), the same "store validates, caller applies the effect" split #208/#210 already use. New `GET /api/daily-spin/:author`, `POST /api/daily-spin/:author/spin`; new `/daily-spin` page (linked from `/coins` and `/support`)
- #212 Reward for consecutive daily logins (Daily Streak) (Tinder) — new `LoginStreakStore` (`loginStreak.ts`): a real 7-day reward cycle (growing day-to-day payouts plus a day-7 jackpot, repeating for longer streaks) idempotent per UTC day — repeat check-ins the same day award no extra coins, matching the issue's "computes rewards idempotently" implementation guide — and reset to day 1 if a day is missed, paid out through #196's `CoinStore.credit()` the same way #211's spin does. New `GET /api/login-streak/:author`, `POST /api/login-streak/:author/check-in`; new `/login-streak` page (linked from `/coins` and `/support`) that fires check-in automatically on load, standing in for a real login event
- #213 Achievement badges and medals (Hinge) — new `AchievementBadgeStore` (`achievementBadges.ts`): a fixed, app-earned badge catalog distinct from #87's user-entered "official achievements" profile field — badges are awarded automatically at real milestones elsewhere in the app (a mutual match, a #212 7-day login streak, a #86 100%-complete profile), never self-claimable, and idempotent (earning a badge twice is a no-op, and a badge once earned is never revoked). New `GET /api/achievement-badges` (catalog), `GET /api/achievement-badges/:author` (earned); new `/achievements` page (linked from `/support` and `/login-streak`)
- #214 Daily polls focused on relationships and personality (OkCupid) — new `DailyPollStore` (`dailyPolls.ts`): distinct from #66's permanent, self-selected profile prompts and #94/#100's private per-pair compatibility score, this is one shared multiple-choice question everyone answers on a given UTC day (a fixed catalog cycled deterministically by day, no admin scheduling), showing the real aggregate "how did everyone else answer" percentage breakdown once the viewer has voted — one vote per author per day. New `GET /api/daily-poll/:author`, `POST /api/daily-poll/:author/vote`; new `/daily-poll` page (linked from `/support`)
- #215 Two-person quiz to compare views before chatting (Hinge) — new `CoupleQuizStore` (`coupleQuiz.ts`): distinct from #214's public, everyone-vs-everyone poll, this is a private session between exactly two named authors (one quiz per unordered pair, restarting it just returns the existing session) answering the same 5 fixed relationship-views questions independently — both sides' actual answers and a real per-pair agreement percentage are revealed only once *both* have submitted, so neither side can anchor on the other's answers. New `POST /api/couple-quiz/start`, `POST /api/couple-quiz/:quizId/answers`, `GET /api/couple-quiz/:quizId?author=`; new `/couple-quiz` page (linked from `/support`)
- #216 Speed Dating event at set times of the week (Match.com) — new `SpeedDatingStore` (`speedDating.ts`): distinct from #155's generic LiveEventStore (any author schedules a one-off event, subscribers just get notified when it starts), this is a fixed recurring weekly UTC slot with a real "circle method" round-robin scheduler pairing every joined participant with every other participant across sequential timed rounds. Expressing interest is only allowed about someone you were actually paired with in a round, and — the same "only visible once mutual" rule #94's swipe matching uses — only becomes a visible match once both sides express it. New `GET /api/speed-dating/next-session`, `POST /api/speed-dating/join`, `POST /api/speed-dating/:sessionKey/generate-rounds`, `POST /api/speed-dating/:sessionKey/interest`, `GET /api/speed-dating/:sessionKey/matches?author=`; new `/speed-dating` page (linked from `/support`)
- #217 Blind Chat without seeing photos for the first few minutes (Hinge) — new `BlindChatStore` (`blindChat.ts`): a real per-pair 10-minute timer that decides whether the client may render photos yet — the same "server computes a boolean, client applies the blur" split #144's photoWarning.ts already uses, rather than actually withholding the photo data. Either side can request an early reveal, but it only takes effect once *both* have asked, the same mutual-consent shape #215's couple quiz and #216's speed-dating interest already use. New `POST /api/blind-chat/start`, `POST /api/blind-chat/reveal-request`, `GET /api/blind-chat/status?author=&partner=`; new `/blind-chat` page (linked from `/support`)
- #218 Scoring system for positive behavior (Karma / Respect Score) (Tinder) — new `KarmaScoreStore` (`karmaScore.ts`): everyone starts in good standing at 80/100 with room to move in both directions from real signals this app already tracks — being reported (#41) or blocked (#42) docks points, a mutual match (#94's swipe matching) rewards a small amount toward a perfect 100. Each real-world event (a specific report id, a specific blocker/blocked pair, a specific match pair) is only ever applied once, so a retry or re-derivation never double-penalizes or double-rewards. New `GET /api/karma/:author`; new `/karma` page (linked from `/support`)
- #219 Daily challenges (e.g., complete 3 prompt answers or send 2 voice notes) (Coffee Meets Bagel) — new `DailyChallengeStore` (`dailyChallenges.ts`): a fixed daily checklist (swipe on 10 profiles, send 5 messages, send 2 voice notes, answer 3 profile prompts), reset at UTC midnight, whose progress is driven entirely by real events already happening elsewhere (swipes, chat messages, voice-note sends, saved prompt answers) rather than a self-reported counter. Completing a challenge requires an explicit claim (idempotent per challenge per day) which credits coins via #196's `CoinStore.credit()`. New `GET /api/daily-challenges/:author`, `POST /api/daily-challenges/:author/claim`; new `/daily-challenges` page (linked from `/support`)
- #220 Weekly leaderboards based on activity/popularity (Hinge) — new `WeeklyLeaderboardStore` (`weeklyLeaderboard.ts`): distinct from #95's lifetime, never-reset SmartScoreStore, this tracks the same two signals (activity = swiping, popularity = being liked) but scoped to a real rolling 7-day window that resets automatically at each epoch-week boundary, no admin cron needed. Popularity is weighted double raw activity in the combined score — an arbitrary, documented choice, since neither reference app ever published a real weighting. New `GET /api/leaderboard/weekly?limit=`, `GET /api/leaderboard/weekly/:author` (rank); new `/leaderboard` page (linked from `/support`)
- #221 Create online group events (webinars, games) (Match.com) — new `GroupEventStore` (`groupEvents.ts`): distinct from #155's capacity-free LiveEventStore (a plain "opt in to be notified" subscription), this is a real capacity-limited RSVP with a waitlist — reaching capacity waitlists rather than rejecting, and cancelling a confirmed spot automatically promotes the longest-waiting waitlisted author. QR check-in (the other half of the issue's "RSVP/capacity/QR check-in" implementation guide) is #230's separate scope, not duplicated here. New `POST /api/group-events`, `GET /api/group-events`, `GET /api/group-events/:eventId`, `POST /api/group-events/:eventId/rsvp`, `DELETE /api/group-events/:eventId/rsvp`; new `/group-events` page (linked from `/support` and `/live-events`)
- #222 Create real group dates at cafes or in nature (Match.com) — reuses #221's `GroupEventStore` rather than a parallel RSVP engine, since the only real difference between an online webinar/game and an in-person cafe/outdoor date is a physical venue, not a different sign-up mechanic: two new event types (`cafe`, `outdoor`) now require a `location` field (validated, unlike #221's online types where it's absent). Same capacity/waitlist/promotion logic, same routes — `/group-events` page updated with a location field shown only for the in-person types
- #223 Discussion forums or topic-based communities (Community Hubs) (Match.com) — new `ForumStore` (`forums.ts`): topic-based Hubs anyone can browse, but that require joining before posting a thread or reply — each Hub scopes its own membership, threads, and replies rather than one flat feed. New `POST/GET /api/forum-hubs`, `GET /api/forum-hubs/:hubId`, `POST/DELETE /api/forum-hubs/:hubId/members`, `POST/GET /api/forum-hubs/:hubId/threads`, `GET /api/forum-threads/:threadId`, `POST /api/forum-threads/:threadId/replies`; new `/forums` page (linked from `/support`)
- #224 Check-in at public places and see who's there (Match.com) — new `VenueCheckInStore` (`venueCheckIns.ts`): an explicit named-venue check-in, distinct from #108's `CrossedPathsStore` (raw GPS-coordinate proximity never shown as a place) — a single active check-in per person that auto-expires 3 hours after check-in, or ends early via explicit checkout; checking in somewhere new automatically checks you out of the old venue. New `POST /api/venue-check-ins`, `DELETE /api/venue-check-ins/:author`, `GET /api/venue-check-ins/mine/:author`, `GET /api/venue-check-ins/venues`, `GET /api/venue-check-ins/venues/at?venue=`; new `/check-in` page (linked from `/support`)
- #225 Support for podcasts or group audio rooms (Audio Rooms) (Match.com) — new `AudioRoomStore` (`audioRooms.ts`): the real live-membership/moderation half of a Clubhouse-style room (host/speakers/listeners, hand-raising, host-only invite-to-speak and move-to-listener), honest that this app has no SFU to actually mix N-way audio — the same disclosed-limitation pattern #128/#129's `CallStore` uses for missing TURN relay. When the host leaves, the longest-standing speaker is promoted to host, or the room closes if no speakers remain. New `POST/GET /api/audio-rooms`, `GET /api/audio-rooms/:roomId`, `POST /api/audio-rooms/:roomId/join`, `POST /api/audio-rooms/:roomId/leave`, `POST /api/audio-rooms/:roomId/raise-hand`, `POST /api/audio-rooms/:roomId/invite-to-speak`, `POST /api/audio-rooms/:roomId/move-to-listener`, `DELETE /api/audio-rooms/:roomId`; new `/audio-rooms` page (linked from `/support`)
- #226 Educational blog section with relationship psychology articles (eHarmony) — new `RelationshipBlogStore` (`relationshipBlog.ts`): a seeded catalog of real, substantive psychology articles across 6 categories (attachment styles, communication, conflict resolution, red flags, long-term commitment, dating tips), extendable through a real admin-gated publish endpoint rather than a fixed, never-growing list; reading an article's full body counts as a read for its `readCount`. New `GET /api/blog/articles?category=`, `GET /api/blog/articles/:articleId`, admin-only `POST /api/admin/blog/articles`; new `/blog` page (linked from `/support`)
- #227 Local singles events calendar (Match.com) — new `LocalSinglesEventStore` (`localSinglesEvents.ts`): admin-curated local mixers (gated by `requireAdmin`, unlike #221/#222's member-hosted `GroupEventStore`), browsable by city and calendar month, reusing the same real capacity-limited RSVP-with-waitlist mechanic #221/#222 established. New admin-only `POST /api/admin/local-events`, `GET /api/local-events?city=&month=`, `GET /api/local-events/cities`, `GET /api/local-events/:eventId`, `POST /api/local-events/:eventId/rsvp`, `DELETE /api/local-events/:eventId/rsvp`; new `/local-events` page (linked from `/support`)
- #228 Ability to form interest groups (e.g., a hiking group) (Match.com) — new `InterestGroupStore` (`interestGroups.ts`): a persistent, joinable membership circle around a shared interest, distinct from #223's discussion-only `ForumStore` (no threads/replies here) and from #221/#222's one-off `GroupEventStore` (no standing membership there) — members can organize their own capacity-limited RSVP'd Activities restricted to the group, reusing the same waitlist/promotion mechanic #221/#222/#227 established. New `POST/GET /api/interest-groups`, `GET /api/interest-groups/:groupId`, `POST/DELETE /api/interest-groups/:groupId/members`, `POST/GET /api/interest-groups/:groupId/activities`, `POST/DELETE /api/interest-group-activities/:activityId/rsvp`; new `/interest-groups` page (linked from `/support`)
- #229 Ability to post reviews and experiences from good date spots (Match.com) — new `DateSpotReviewStore` (`dateSpotReviews.ts`): a real Yelp-style named-venue review with a 1-5 rating and free-text experience, distinct from #224's live, expiring "I'm here now" `VenueCheckInStore` and #147's fixed activity-*category* `dateProposals.ts` catalog. One author posting again for the same venue edits their existing review rather than padding the average with a second entry. New `POST /api/date-spot-reviews`, `GET /api/date-spot-reviews?venue=`, `GET /api/date-spot-reviews/venues`; new `/date-spot-reviews` page (linked from `/support`)
- #230 Ability to confirm event attendance with a QR code (Match.com) — new `EventCheckInStore` (`eventCheckIns.ts`): the QR check-in half of the "RSVP/capacity/QR check-in" implementation guide #221/#222/#227/#228 each explicitly deferred to here — a generic per-(event, attendee) ticket usable against any of those RSVP'd event types, rendered as a real scannable QR code reusing #26's `qrcode` package. Only the host who issued a ticket can confirm it, and a ticket can only be checked in once. New `POST /api/event-check-ins/tickets`, `GET /api/event-check-ins/tickets/:eventId/:attendee`, `GET /api/event-check-ins/:eventId`, `POST /api/event-check-ins/confirm`; new `/event-check-in` page (linked from `/support`)
- #231 Personal AI assistant for writing an optimized bio (Hinge) — new `bioOptimizer.ts` (`analyzeBio()`): an honest, deterministic rule-based feedback engine, not a fabricated LLM integration this app has no model/API key for — same scoping call as #120's `bioAnalysis.ts`. Scores length, presence of a reply-inviting question, generic cliché phrases, and specific-keyword density, with every point of the score traceable to a concrete, disclosed rule and every tip naming the exact thing to change. Stateless `POST /api/bio-optimizer/analyze`; new `/bio-optimizer` page (linked from `/support`)
- #232 Automatic suggestion of the best photos based on others' feedback (Hinge) — new `bestPhotoSuggestion.ts` (`suggestBestPhoto()`): ranks an album's existing photos by #112's real like/note engagement signal (`PhotoInteractionStore`), not a fabricated computer-vision "photo quality" model this app has no inference infrastructure for. Only suggests once the album has real feedback to act on and there's more than one photo to choose between, otherwise honestly reports "not enough feedback yet." New `GET /api/photos/:owner/best-photo-suggestion`; `ProfilePhotoGallery.tsx` shows a "make it primary" banner to the album's own owner when a better-performing photo isn't already first, reusing #62's existing reorder endpoint
- #233 Smart analysis of personality compatibility based on conversations (OkCupid) — new `conversationCompatibility.ts` (`analyzeConversationCompatibility()`): an honest, deterministic analysis of the two people's *actual* chat history, not a fabricated LLM personality profiler — distinct from #94/#118-120's static profile-based compatibility, which never looks at how two people actually talk. Three equally-weighted, explainable signals: conversation balance, mutual question-asking ("curiosity"), and topic overlap (reusing #94's `computeInterestCompatibility` over each side's extracted keywords); average message length is reported but deliberately excluded from the score. Reports "not enough data" below a minimum-messages-per-side floor rather than guessing. New `GET /api/rooms/:roomId/conversation-compatibility?authorA=&authorB=`; new `/conversation-compatibility` page (linked from `/support`)
- #234 Guide chatbot for advice before the first date (Hinge) — new `firstDateGuide.ts`: a keyword-matching decision-tree guide over a curated, genuinely useful topic catalog (where to meet, what to wear, conversation starters, nerves, timing, who pays, safety), not a fabricated open-ended LLM chatbot this app has no model for — same scoping call as #231's `bioOptimizer.ts` and #233's `conversationCompatibility.ts`. A free-text question is matched against each topic's keywords (reusing #120's `extractKeywords`); an unmatched question honestly falls back to the real topic list rather than an invented answer. New `GET /api/first-date-guide/topics`, `GET /api/first-date-guide/topics/:topicId`, `POST /api/first-date-guide/ask`; new `/first-date-guide` page (linked from `/support`)
- #235 Smart summarizer for long conversations (Hinge) — new `conversationSummarizer.ts` (`summarizeConversation()`): a real, deterministic extractive summary — the most frequently discussed keywords (reusing #120's `extractKeywords`) plus a handful of the conversation's own highest-signal messages, shown verbatim in chronological order — rather than a fabricated abstractive LLM summary. A message's "signal" is its own word count plus a disclosed bonus for asking a question. Reports "too short to summarize" below a minimum-message floor. New `GET /api/rooms/:roomId/conversation-summary`; new `/conversation-summary` page (linked from `/support`)
- #236 High-accuracy bot account detection based on typing patterns (Hinge) — new `typingPatternDetector.ts` (`analyzeTypingPattern()`): honestly, this app has no keystroke-level client instrumentation or ML model to fabricate a "typing pattern" score from, so it's scoped to the real behavioral signal already available — the timing and content of a person's own sent messages across all their chats. Distinct from #107's static per-profile `fakeProfileDetector.ts` (report count + spammy bio) — this looks at ongoing chat behavior. Flags impossibly fast and suspiciously uniformly-timed replies, plus the same non-trivial text broadcast verbatim to several different conversations. Admin-gated like #171-186. New `GET /api/admin/typing-pattern/:author`; new `/admin/bot-detection` page (linked from `/admin`)
- #237 Suggest a suitable date location based on shared interests (Match.com) — new `dateLocationSuggestions.ts` (`suggestDateLocations()`): a real, deterministic mapping from every one of #79's 30 catalog interests to one of 8 date-location categories (Outdoor & Active, Food & Drink, Entertainment, etc.), not a fabricated recommendation model or a live places/geocoding API this app has no credentials for — distinct from #147's `dateProposals.ts` (a fixed, non-personalized activity catalog) since this is actually driven by the specific pair's own shared interests, with categories backed by more shared interests surfaced first. New `GET /api/date-location-suggestions?authorA=&authorB=`; new `/date-location-suggestions` page (linked from `/support`)
- #238 Predict relationship success probability (Match Probability Score) (eHarmony) — new `matchProbability.ts` (`computeMatchProbability()`): a weighted composite of every real compatibility signal this app already computes honestly — #94's interest overlap, #118's music match, #119's weekend-plan match, #120's bio keyword match, and #233's actual conversation compatibility when a chat exists — rather than a fabricated single "AI prediction" model. Each signal is only included when both sides actually have real data for it, and the result's confidence level (`low`/`medium`/`high`) honestly reflects how many signals were available rather than presenting every score with equal certainty. New `GET /api/match-probability?authorA=&authorB=&roomId=`; new `/match-probability` page (linked from `/support`)
- #239 Generate custom chat stickers based on the user's face using AI (Hinge) — new `stickerGenerator.ts`/`stickers.ts` (`StickerStore`): this app has no trained face-to-cartoon generative model or image-generation API/credentials, so "AI sticker" is honestly scoped to a real, deterministic image-transform pipeline reusing #82's `jimp` dependency — the user's own uploaded photo (from #45's `PhotoStore`), cropped to a circular sticker frame and run through one of five genuinely distinct real filters (cartoon/sketch/popart/vintage/pixel), not a placeholder. New `GET /api/sticker-styles`, `POST /api/stickers`, `GET /api/stickers/:id` (serves the PNG), `GET /api/stickers/author/:author`; new `/stickers` page (linked from `/support`)
- #240 Smart filter to remove offensive or spam text before sending (OkCupid) — new `messageCleanupFilter.ts` (`cleanMessageBeforeSending()`): an actual pre-send text transformation, distinct from #143's `contentWarning.ts` (an interactive "are you sure?" the sender must confirm past) and `spamDetector.ts`'s own silent post-send flag (text left unchanged) — reuses #176's `filterProfanity` masking and `spamDetector.ts`'s real promo-phrase list to redact profanity, spam phrasing, and URLs, handing back a cleaned preview the sender can choose to send instead. Not a fabricated toxicity-classification model this app has no training data or inference infrastructure for. Stateless `POST /api/messages/clean-preview`; new `/message-cleanup` page (linked from `/support`)
- #241 Full screen reader support (Screen Readers / TalkBack / VoiceOver) (Bumble) — real WCAG/ARIA markup on the core chat experience (`ChatRoom.tsx`), scoped to that primary surface rather than a claim of exhaustive coverage across every one of this repo's ~150 secondary feature pages: the message list is a real `role="log"`/`aria-live="polite"` region so new messages are announced, every icon-only composer button (image/voice/self-destruct-photo/GIF/location/E2EE/game/date-proposal/gift/date-invite) gets a matching `aria-label`, the composer textarea gets a persistent `aria-label` (a placeholder alone disappears once typing starts), a "Skip to message composer" link is focusable at the top of the page, and composer/send buttons get a real 44×44px minimum touch target in `globals.css`. Frontend-only, no backend component — same precedent as #3/#8/#15's other UI-only issues
- #242 Ability to change text font size in the app (Tinder) — new `FontSizeToggle.tsx`: same client-only, localStorage-persisted pattern as #8's `ThemeToggle` (a pure display preference, no account/backend needed). Cycles Default/Small/Large/Extra Large via a `data-font-size` attribute on `<html>`, driving the existing `--chat-font-size` custom property `globals.css` already uses throughout — every element sized off that variable picks up the change automatically, and an explicit choice wins over #3's desktop-breakpoint font bump via CSS specificity regardless of source order. New toggle button in the chat header next to `ThemeToggle`. Frontend-only, no backend component — same precedent as #3/#8/#15/#241
- #243 High Contrast Mode for low-vision users (Bumble) — new `HighContrastToggle.tsx`: same client-only, localStorage-persisted pattern as #8/#242 (a pure display preference, no backend needed). A `data-contrast="high"` attribute drives real WCAG-AA/AAA-exceeding color overrides in `globals.css` — text-on-background contrast raised well past 7:1, and borders switched from a barely-visible ~1.6:1 gray to full black/white — not a cosmetic theme variant, and correctly combines with #8's light/dark theme (a dedicated dark+high-contrast rule is more specific so it always wins when both are active). Chart series colors are left untouched (already CVD-validated for a different WCAG concern, #171). New toggle button in the chat header. Frontend-only, no backend component — same precedent as #3/#8/#15/#241/#242
- #244 Voice command support for performing swipes (Tinder) — new `VoiceSwipeControl.tsx` on `/discover`'s card view: the browser's own real Web Speech API (`SpeechRecognition`), the same honest "real native capability, not a fabricated model" scoping as #4's Notification API — no server-side speech/NLP model exists or is needed. Applies the same "like"/"pass"/"super like"/"rewind" vocabulary as the existing arrow-key swipe shortcuts (#91-#93), just spoken instead of pressed; off by default, toggled on explicitly since continuous mic capture needs real, revocable permission, with graceful `!supported` fallback (renders nothing) in browsers without `SpeechRecognition`. New ambient `speech-recognition.d.ts` (not part of TypeScript's standard DOM lib). Frontend-only, no backend component
- #245 Text descriptions on photos (Alt Text) for accessibility (Bumble) — new `PhotoAltTextStore` (`photoAltText.ts`): a real, author-written description per photo that a screen reader actually announces, not a fabricated image-captioning model — same keyed-by-owner+photoId decoupling as #112's `PhotoInteractionStore`. `ProfilePhotoGallery.tsx`'s `<img>` now renders the real alt text (falling back to `"{owner}'s profile photo"` when none is set) instead of a generic `"Profile"` string, and the album's own owner gets an inline editor to write or update it. New `PUT`/`GET /api/photos/:owner/:photoId/alt-text`
- #246 Automatic captions for video calls (Tinder) — new `CallCaptions.tsx`: the browser's own real Web Speech API (`SpeechRecognition`) transcribing each participant's own microphone locally, the same honest "real native capability, not a fabricated model" scoping as #244's `VoiceSwipeControl`, with the resulting text relayed to the other participant over a new `call:caption` socket event so each side sees the other's live transcript overlaid on the video panel. The server only relays already-transcribed text between the two call participants once `#128/#129`'s `CallStore` reports the call `active` (never during ringing), the same relay-not-process division of responsibility as `call:signal`'s WebRTC signaling. Off by default and only listens while explicitly toggled on. Frontend: `ChatRoom.tsx`; Backend: `server.ts`
- #247 Buttons and elements designed with standard touch target size (Bumble) — a real global WCAG 2.1 AA baseline in `globals.css` (`button, input[type="button"|"submit"|"reset"] { min-height: 44px }`) applied across every one of this repo's ~150 pages, not just the 2-3 composer classes #241 sized by hand — text buttons already have comfortable width from their own padding+label, so only height needed a floor; a new `.chat-app__touch-target-exempt` opt-out exists for a control that's deliberately part of a larger, already-compliant tap target. Icon-only square "✕" dismiss buttons (which need width too) got an explicit `.chat-app__icon-close-button` class in `ChatRoom.tsx`, `GifPicker.tsx`, `LocationPicker.tsx`, `DateInvitePicker.tsx`, and `AchievementsInfoEditor.tsx`. Frontend-only, no backend component — same precedent as #3/#8/#15/#241-243
- #248 Haptic feedback on like or Match (Bumble) — wires the browser's real Vibration API (`navigator.vibrate`, already defined as `vibrateForeground`/`vibrationPatternForCategory` in `notificationSound.ts` for #160's push vibration) directly into the swipe action itself: `/discover`'s and `/liked-you`'s `swipe()` now vibrate a short single pulse on a plain like/super-like, or the longer celebratory double-buzz on an actual match — the same per-category patterns #160 already uses for push notifications, so the feel is consistent whether the buzz comes from your own swipe or an async push. Only Android Chrome/Firefox actually implement the Vibration API (iOS Safari has none), an honest disclosed platform gap rather than a fabricated cross-platform haptics layer. Frontend-only, no backend component
- #249 One-Handed Mode for easy use (Bumble) — new `OneHandedModeToggle.tsx`: same client-only, localStorage-persisted display-preference pattern as #8/#242/#243. A `data-one-handed="true"` attribute on `<html>` drives real CSS in `globals.css` that shrinks and bottom-anchors the whole chat surface into a genuine one-thumb-reachable corner (like a phone OS's own one-handed mode), with its own internal scroll — rather than an exhaustive per-control reachability redesign across every one of this repo's ~150 pages, the same "core chat experience" scoping precedent as #241. New toggle button in the chat header next to `HighContrastToggle`. Frontend-only, no backend component
- #250 Full keyboard navigation support in the browser (Bumble) — two real, testable gaps closed on the core chat experience: (1) `ChatRoom.tsx`'s existing global Escape handler (previously only closed the shortcuts help) now dismisses every open dialog/picker — GIF, location, date-proposal, date-invite, gift, report — and the search panel, the ARIA Authoring Practices' expected way out of a dialog for a keyboard-only user who'd otherwise have to tab all the way to a visible close button; (2) a real, visible `:focus-visible` outline (WCAG 2.1 SC 2.4.7) added globally in `globals.css`, since browsers' own default focus rings are inconsistent and easy to miss. Distinct from #16's power-user hotkeys (Ctrl+K, Shift+Enter, `?`) and #241's screen-reader ARIA markup — this is about operability via keyboard alone. Frontend-only, no backend component
- #251 Ability to add background music to the profile (Hinge) — new `BackgroundMusicStore` (`backgroundMusic.ts`): same one-per-profile, replace-on-reupload shape as #64's `VoiceIntroStore`, but for an uploaded music file (with an optional title) rather than a mic recording, since this app has no licensed streaming catalog to pull real songs from — distinct from #77's `SpotifyInfoStore`, which only ever stores track *metadata*, never audio. New `BackgroundMusicUploader.tsx` on `/settings/profile` (file upload, not `MediaRecorder`) and read-only `BackgroundMusicPlayer.tsx` on `/profile/[author]` (a real play button, since browsers block unmuted autoplay without a user gesture). New `POST`/`GET`/`DELETE /api/background-music(/:author)`
- #252 Ability to measure user activity level as a percentage (Tinder) — new `activityLevel.ts` (`computeActivityPercentile()`): a real normalized comparative percentage — what percentage of other tracked authors this author is at least as active as — distinct from #95's `SmartScoreStore` raw absolute swipe count/Elo rating and #220's ordinal weekly leaderboard rank, both of which it reuses data from rather than tracking a third parallel activity counter (`SmartScoreStore` gained a new `getAllActivityCounts()` getter). Scoped to authors who've actually swiped at least once — this app has no central "every user" roster to compare against, the same real-data-only scope #220's leaderboard already accepts. New `GET /api/activity-level/:author`; new `ActivityLevelDisplay.tsx` on `/settings/profile`, shown only to the profile owner like #95's `SmartScoreDisplay`
- #253 Show Facebook mutual connections if the account is linked (Tinder) — new `FacebookConnectStore` (`facebookConnect.ts`): the real thing, not a duplicate of #114's `ContactsGraphStore` workaround (#114 explicitly rebuilt this same "mutual friends" signal via uploaded phone contacts *because* this app had no Facebook Graph API integration wired up for it — this issue is that integration). Reuses #24's `FacebookAuthService` to verify the access token, and a new `fetchFacebookFriends()` calls the Graph API's real `/me/friends` — honestly disclosed that Facebook's own 2015 platform lockdown restricts this to only the subset of friends who've *also* authorized this app, not a full social-graph import. New `POST`/`GET`/`DELETE /api/facebook-connect(/:author)`, `PUT /api/facebook-connect/:author/hide`, `GET /api/facebook-mutual-connections/:author` (same drawn-from-the-same-pool shape as #114); new `FacebookConnect.tsx` on `/settings/profile` and read-only `FacebookMutualConnections.tsx` on `/discover` (mutual *counts* only, never the raw friend-id list)
- #254 System showing a user's message response speed (Tinder) — new `responseSpeed.ts` (`computeResponseSpeed()`): a real median reply gap computed from an author's own actual message timestamps across every room they've sent a message in, same cross-room aggregation as #236's `typingPatternDetector.ts`, not a fabricated estimate — a "response" only counts where the immediately preceding message in that room came from someone else, so consecutive messages from the same author (adding a thought) aren't misread as a fast reply. New `GET /api/response-speed/:author` (not admin-gated, since unlike #95's deliberately-hidden Smart Score this is meant to be a real profile badge); new `ResponseSpeedBadge.tsx` on `/profile/[author]` — positive-only, same "highlight a good trait, don't shame a bad one" norm real dating apps use, so a "slow" or "unknown" result renders nothing
- #255 Ability to measure zodiac sign compatibility by birth month (Hinge) — new `zodiacCompatibility.ts` (`computeZodiacCompatibility()`): a real, published Western-astrology element-compatibility formula (same-element pairs score highest, Fire/Air and Earth/Water are the classic complementary pairs, Fire/Water and Earth/Air are the classic clashing pairs, everything else neutral) — an honest, deterministic, disclosed formula, not a fabricated model. Reuses #72's `ZodiacInfoStore`'s already-computed sign rather than a second birth-date parser. New `GET /api/zodiac-matches/:author` (same drawn-from-the-same-pool shape as #119's weekend-plan matches, filtered to non-clashing pairs); new `ZodiacMatches.tsx` "♈ Zodiac compatibility" section on `/discover`
- #256 Send voice notes with adjustable playback speed (1x, 1.5x, 2x) (Tinder) — new `VoiceNotePlayer.tsx` wraps #122's existing waveform+`<audio>` voice-note rendering with the browser's own real `HTMLMediaElement.playbackRate`, the same "real native capability" scoping as #244's `VoiceSwipeControl`. A tap cycles 1x → 1.5x → 2x → 1x; the chosen speed is remembered (localStorage) across every voice note in the app rather than resetting per message, the same "set it once" UX real chat apps use for this. Frontend-only, no backend component
- #257 Filter profiles by favorite pets (OkCupid) — extends #96/#97's `DiscoveryFiltersStore`/`candidateMatchesFilters()` with a `requiredPets` list, reading #75's `PetsInfoStore` pet data the same way #97 reads lifestyle data (raw data regardless of hide flags, missing data fails an active filter) — a candidate matches if they have at least one of the required pet types, same "any of" semantics as #96's required-languages filter. Same `PUT`/`GET /api/discovery-filters/:author` routes and `DiscoveryFiltersEditor` UI gain the new pet-picker control
- #258 Ability to define "my goal this week" in the profile (Tinder) — new `WeeklyGoalStore` (`weeklyGoal.ts`): distinct from #29's one-time onboarding relationship-intent choice, a real short free-text statement that genuinely resets every calendar week with no admin cron needed — same real epoch-week boundary #220's `WeeklyLeaderboardStore` already uses, so a goal set in a previous week reads back as unset rather than lingering stale text. New `PUT`/`GET /api/weekly-goal/:author`; wired into #81's `buildProfilePreview()` alongside bio so it shows on `/profile/[author]` and the self-`ProfilePreview`; new `WeeklyGoalEditor.tsx` on `/settings/profile`
- #259 Show a timer for time spent together after matching (Tinder) — new `MatchTimerStore` (`matchTimer.ts`): an ascending "how long have we been matched" duration, the opposite framing of #136/#137's descending time-to-respond countdown. Deliberately its own store rather than reusing `MatchExpiryStore`'s `matchedAt`, since #137's `extend()` shifts that field forward by a full 24h window (correct for a response deadline, but would understate real elapsed time here) — this store records the one true, never-mutated match moment. New `GET /api/matches/:author/:candidate/time-together`; new `MatchTimer.tsx` "💞 Matched Xd Xh ago" on `/matches`, which — unlike `MatchCountdown` — never disappears once messaging starts
- #260 Ability to block a specific email domain (Bumble) — new `EmailDomainBlockStore` (`emailDomainBlock.ts`): same self-declared-identity, sweep-block shape as #43's `ContactBlockStore` (phone contacts) — an author self-declares their own email, and any other author can name a domain (e.g. a workplace's) to sweep-block everyone currently registered under it via the shared `BlockStore`. Same disclosed one-shot-sweep limitation as #43: it only catches authors already registered with a matching domain at block time, not future signups. New `POST /api/profile/email`, `POST /api/email-domains/block`; new "Block email domains" section in `ChatRoom.tsx` next to #43's phone-contact blocking
- #261 Home screen widget support for iPhone and Android (Tinder) — same genuinely platform-specific gap as #193/#194's native app-store billing bridges, not a "web equivalent" situation: this repo has no native iOS WidgetKit extension or Android AppWidgetProvider to actually render on a home screen. New `widgetSummary.ts` (`buildWidgetSummary()`) is the real, platform-agnostic backend piece any such widget's timeline provider would poll — a small snapshot built from this app's actual live data (#159's unread-notification count, #103's pending-likes count, #100's active-match count), not fabricated sample data. New `GET /api/widget-summary/:author` — no web UI, since there's no home screen for a browser tab to add a widget to
